# 变更记录

版本号遵循 `主.次.修`。日期为发布日。
完整的安装包见 [Releases](https://github.com/Hector-xue/IvyeaNote/releases)。

---

## v0.11.28 — 2026-09-12

**账本被克隆一次，这一轮的墓碑 / 附件哈希 / 游标就丢：「本地少了 184 篇」与附件反复重推的根因。**

v0.11.27 收尾时手机上一直挂着「本地少了 184 篇，已暂停删除」——那 184 条正是云端刚打了
delete 的 `.txt` 副本。手机明明拉到了删除（版本号记成了 2），却没记墓碑，于是每轮都把它们
算成"已知但本地没有"。同一个机制也解释了更早的"附件每次同步都重推一遍"（`assets` 丢了）。

- 根因：v0.11.25 起**每次启动都对齐一次库清单**（`relink` → `linkVaults`），而 `linkVaults`
  对服务端认得的库写的是 `{ ...had, name }`——**克隆**。启动时的自动同步正拿着旧对象在改账本：
  `versions[p] = v` 是就地改、克隆体共享看得见；`tombstones = {…}` / `assets = {…}` / `cursor = n`
  是整体替换、只落在旧对象上；persist 存的是克隆体，于是版本号在、墓碑没了。
- 修法三层：`linkVaults` 已有的库**保持同一个对象**，名字变了就地改；同步引擎写墓碑 / 附件哈希改成
  就地写入（`(meta.tombstones ??= {})[p] = v`）；同步结束落盘走 `commitLedger(live, used)`——
  state 里的对象和引擎用的不是同一个时把六个账本字段搬过去，相同则不动。
- 回归测试三条（克隆体看得到墓碑与游标 / 就地写入共享 / linkVaults 保持对象身份），
  换回旧代码两条确实红。
- 用户侧：横幅上点「确实是我删的」是安全的——那些路径云端本来就已删除，推上去只是重复标记。

---

## v0.11.27 — 2026-09-12

**安卓换库位置事故：`CNC.md` 落盘成 `CNC.md.txt`，每同步一轮多一份副本，整个界面冻住。**

### 事故复盘

用户（2026-09-12 凌晨）把手机上的库换到 `Documents/obsidian`：选完文件夹过了很久才弹出
「移动笔记」确认框，之后整个应用卡死、按钮没反应；缓过来一看每篇笔记有 4～5 份——
`AI记账APP.md`、`AI记账APP.md (1)`、`(2)`、`(3)`，全挂着 `TXT` 角标。云端库里查到
194 条 head，其中 122 条是 `*.txt`。三个 bug 叠在一起：

1. **给 `createDocument` 的 MIME 是 `text/plain`。** Android 的 ExternalStorageProvider 建文件时
   会拿请求名的后缀反查 MIME 和我们传的比（`FileUtils.splitFileName`）：对不上就把整个请求名
   当基名、再补上 MIME 对应的后缀——`CNC.md` 落盘成 `CNC.md.txt`。插件却照请求的名字往缓存里记，
   当场看不出来。
2. **缓存未命中就整树重扫。** 重扫之后缓存变成磁盘真相（只有 `CNC.md.txt`），`CNC.md` 消失；
   下一次写它又走 create，系统遇到同名再加 ` (1)`……而同步引擎把这些 `.txt` 全当新附件推上云端，
   再把云端 `CNC.md` 拉回来，如此往复。
3. **所有 SAF 操作跑在安卓主线程上**（Tauri 插件命令经 `run_on_android_context` 派发到 UI 线程）。
   迁移几十篇 = 几十次整树 BFS + 全部文件 IO 都压在 UI 线程，界面自然纹丝不动。

### 修法（`plugins/ivnote-saf` Kotlin）

- **MIME 按这台机器的 `MimeTypeMap` 给**：后缀查得到就传查到的那个（必然对得上）；查不到就传
  `application/octet-stream`——`splitFileName` 对这个值不补后缀，名字原样落盘。
- **建完核对系统真正落下的名字**：和请求名不一致就把刚建的空文件删掉、报错（`系统把「x」落成了「y」`）。
  宁可这一次写失败让人看见，也不能让磁盘和缓存各说各话。目录同样核对。
- **未命中只沿路径逐级查**（每层一个 cursor，顺手把沿途目录的子项填进缓存），不再整树重扫。
- **所有磁盘活挪到单线程 worker 上**，主线程只收命令、起目录选择器。命令顺序不变（单线程 FIFO）。
- 本机用 kotlinc 2.1 + 手写 Android/Tauri 桩做了类型检查（`-language-version 1.9`，与工程一致），
  真正的编译门禁仍是 CI 的 `apk` job。

### JS 侧兜底

- `migrateFiles` 复制完**回头核对目标端的 list**：少一个就整体失败，调用方不切 `localPath`，
  笔记留在原地。回归测试用一个"写入时改名"的假存储验过：修复前该用例确实红。

### 事故清理

- 云端 `obsidian` 库里 122 条 `*.md.txt` / `*.md (n).txt` 按同步协议打上 delete（与 `ApplyPush`
  同一套语义：heads 标 deleted、changes 追加一条 delete），各端下次同步会把它们移进本机回收站。
- 手机上更新到本版后同步一次：`.txt` 副本进回收站；同步面板会提示「本地少了 71 篇」——点
  **「从云端拉回来」**，这次会以正确的名字落盘。**别在旧版上点这个按钮**（旧版又会写出 `.txt`）。

---

## v0.11.26 — 2026-09-11

**两条真机反馈。**

- **状态栏「AI」菜单压住状态栏**（用户截图：「整理排版」盖住了「插入图片」那一行）。
  菜单此前从按钮顶边往下铺，放不下只是被推到贴底。`ContextMenu` 加 `flipY`：下方放不下
  就整张翻到状态栏之上。`verify-ui` 新增一条量菜单底边 ≤ 状态栏顶边。
- **手机上选完文件夹没反应**（新建笔记库 / 设置里换位置都是）。选目录用的是系统的
  `ACTION_OPEN_DOCUMENT_TREE`——另一个 Activity。回来时主 Activity 可能已经被系统回收重建
  （内存紧、"不保留活动"），WebView 一重载，等结果的那个 Promise 就没了，界面上自然什么
  都不发生。改成两段式：Kotlin 侧把选中的目录同时落到 SharedPreferences
  （新命令 `takePendingPick`，领走即清空）；JS 侧发起前记下"我要干什么"（新建 / 绑到哪个库），
  启动时两边都在、且结果晚于意图、且不超过 15 分钟 → 把没走完的那半段接着走完。
  取消也不再静默：会说「没有选择文件夹」。本机编不了 Kotlin，这条只有真机能最终确认。

---

## v0.11.25 — 2026-09-11

**换库位置再也删不掉东西；库能切、能删、名字就是文件夹名。**

### 事故复盘

用户在手机上「绑定本地目录」→ 所有文件消失 → 同步把电脑上的也全删了 → 切回默认才回来。
链条（逐段核实）：

1. **触发点是个 bug**：`onBindFolder` 里 `allPaths.length` 取的是函数形参个数 = 0，
   于是「移动 N 个文件」的确认框从不弹、迁移从不跑，库直接指向那个**空**目录
   （v0.10.2 起如此，v0.11.24 已修）。
2. **同步把"本地没有"当成"用户删了"**：账本只按库 id 存，不知道自己在哪个物理位置
   对的账，换了位置照样拿老账本对新目录 → 全库 delete 一轮推上云端。
3. 电脑 pull 到 delete 直接物理删（v0.11.24 起会先进回收站）。
4. 切回默认能救回来是侥幸：内部存储里文件还在，以删除版本为 base 推 upsert 就复活了。

只修触发点不够——账本不认位置这个设计还在，任何一次位置变化（换目录 / SAF 授权失效 /
SD 卡拔掉）都会重演。这一版根治：

### 账本跟着位置走

`VaultMeta.syncedAt` 记下这本账是在哪个 localPath 上对出来的。位置不一致时引擎把账本
清零、当成一台新设备从头对账（**只对账云端最终状态**，不逐条回放历史——逐条回放会把
中间版本一个个砸到新位置的文件上，留下一堆莫名其妙的 `.conflict-` 文件）：云端有本地
没有 → 落盘；两边都有且不同 → 冲突副本，不覆盖任何一边；本地有云端没有 → 推上去；
回放里的历史 delete 碰到新位置本来就有的文件 → 留着、推回去。
**换位置只会让文件变多，不会变少。** 老账本没有这个字段 → 直接记成当前位置，升级本身
不触发全量回放。

### 删除熔断

位置没变、但一轮里本地少了 **≥ 30% 且 ≥ 10 篇**（或全部、库里 ≥ 3 篇）——不像是人一篇篇
删的，更像目录不见了：**这一轮不推删除**，其它改动照常。状态栏变红「本地少了 N 篇，
已暂停删除」（手机端红条带「查看」），同步状态面板里列出清单和两条路：
「从云端拉回来」（`.md` 直接从账本里的上次同步内容写回，附件按哈希取 blob，不用等网络）
/「这些确实是我删的，推上去」（只放行这一轮）。Dropbox / Obsidian Sync 都有这道闸。

验证不只在内存假件上：`sync.e2e.test.ts` 新增一条**把事故原样重演**的用例——真 Go 服务端 +
真目录，设备 P 换到空目录后同步、设备 Q 再同步。老引擎跑这条：云端出现 3 条 delete
（红）；新引擎：0 条、P2 里全部文件回来、Q 一篇不少、清空目录后熔断生效（绿）。

### 库管理：能切、能删、名字就是文件夹名

用户原话：「电脑端新建库，旧库也没有地方可以切回去，也没有选择库的地方，应该也没有
删除库的地方」「手机上只有新建库的功能，没有删除选项，有些测试用的空白库都一直存在」
「我认为库名就应该是文件夹名字」。

- **新建笔记库 = 选一个文件夹**（桌面系统对话框，里面能新建；安卓 SAF 选目录）；
  库名 = 文件夹名；选中的文件夹里已有 `.md` 就直接当库用（Obsidian 的 "Open folder as
  vault"）。只有浏览器版（没有磁盘）还是问名字、落内部存储。此前新建默认落 `opfs://`
  ——安卓在 `/data/data/…/app_webview/` 里，文件管理器根本看不到（用户：「我压根找不到
  源文件在手机上的哪个位置」）。
- **桌面库名下拉列出全部库**（每个带位置），点了就切；手机那张单子也带位置。
  顺手删掉了一个 v0.11.9 起就声明了却从没渲染过的 `vaultSelector` 属性。
- **删除笔记库**（桌面下拉 / 手机库单子）：**用户的文件一个都不碰**——绑了文件夹的只从列表
  去掉，文件原地留着；只有应用内部存储那份会清掉。云端库同时在服务端打**软删除**
  （`DELETE /vaults/{id}`：列表不再有它、同步一律 403、内容原样留着可审计可撤销），
  否则下次对齐列表它又回来。
- **别的设备删掉的库，这台设备跟着放手**：服务端把已删除 id 单独列出来
  （`GET /vaults` 多一个 `deleted`），客户端对齐列表时不再把它当孤儿"收养"进别的库
  （那会让删掉的库以另一个 id 复活）。绑了文件夹的直接去掉；存在内部存储且有内容的转成
  本地库留着。登录着的话**每次启动对齐一次**，不用等它被选中撞 403。
- 绑了文件夹的库显示名 = 文件夹名（SAF 的 `content://` 换算成 `Documents/IvyeaNote`
  这种人能读的）；绑定时库名跟着改，云端同步改名（`PATCH /vaults/{id}`）。
- 换位置的迁移现在**连附件一起搬**：此前只搬 `.md`，图片 / PDF 全留在内部存储里，
  新位置一打开全是裂图。

### 验证

vitest：`sync.test.ts` 新增 S1/S1b/S1c/S5（换位置）与 S2/S3/S4（熔断）——逐条在旧引擎
上验过是红的；`vaultLink.test.ts` 新增已删除库四条；`vaultName.test.ts`；
`App.integration` 新增库切换 / 删除 / 显示名 / 别的设备删库 / 熔断面板五条。
Go：软删除 / 改名 / 迁移幂等三条。真服务端 e2e 一条。`verify-ui` 139/139。

---

## v0.11.24 — 2026-09-11

**误改能找回、误删不再全端消失、HTML 工具能直接用、状态栏不再压页面。**

### 文件历史：误改的内容找得回来

用户原话：「如果不小心在其中一端误删了或者误修改了文件然后又全端同步了，别的端也被
同步删了，怎么办？现在只有在回收站里面找到，但是被误修改的可真就无法找回了，
有好的 git 或者备份方案吗？」

先说一个事实：**每一版从来都没丢过**。同步协议从第一天起就规定 changes 流只追加、
blob 内容寻址不删（`shared/protocol.md` §5）——每次推上去的每一版都躺在服务端里。
缺的不是数据，是把它拿回来的那条路。这一版把路修出来，分两层：

- **云端版本**：服务端新增两条只读接口（`/sync/history`、`/sync/deleted`），把某路径的
  历史版本、当前已删除的文件列出来。**恢复不需要新接口**：把旧版内容写回本地，下一轮
  push 就是一次普通 upsert，全端跟着收敛——和用户手动改回去走的是同一条路。
- **本机快照**：不登录也有。每次写盘**之前**把盘上的旧内容存进 `.ivyea/history/`
  （同一篇 5 分钟一张、留 30 天、每篇最多 50 张；`.ivyea/` 本来就不进同步、不进树、
  不进索引）。存的是**覆盖之前**的内容而不是刚写下的：Obsidian 那种"定时存当前内容"
  的做法，打开一篇第一下就删错一段、0.8 秒后自动落盘——第一张快照已经是删错以后
  的样子。三方自动合并改写本地那份之前也留一张。

两层合成一条时间线，在**右栏「历史」标签**（手机在「⋯」里）：每行写着多久以前、
来自本机快照还是云端第几版、是不是这台设备推的；选一版看**对照**（红 = 恢复后去掉、
绿 = 加回）或全文，一键「恢复到这一版」。恢复前会**强制**给现在这版留底（不受 5 分钟
节流），所以恢复错了还能再恢复回来——没有任何一步是不可逆的。命令面板也有
「查看文件历史」。

关于 git：库就是一个普通文件夹，愿意的话 `git init` 进去照常用（`.git/` 会被文件树
隐藏）；但对"误改找回"这件事，上面这套不用记 commit、不用装东西、手机上也有。

### 误删：别的设备删的，同步过来先进回收站

此前回收站只在**删除的那台设备**上有：手机上误删一篇，电脑 pull 到 delete 就直接
物理删了，电脑的回收站里什么都没有。现在同步引擎收到远端删除时先把本地那份挪进
自己的 `.trash/`（附件同样），再从原路径拿掉——每台设备都有一份可恢复的。

回收站面板还多了第二段**「云端已删除」**：在别的设备上删掉、这台机器上没有的文件
（换了台新机器的场景），点一下从云端找回到原位置。

### HTML 工具能直接在笔记里用

用户把 AI 生成的「划线价与 BD 维护助手」存进库里，点开只有半句被状态栏压住的
「此工具需要浏览器允许 JavaScript 才能本」。「这种工具类的 HTML 能做到直接在笔记
里面使用吗？」能：

- 带脚本的页面在工具条下给一条明面上的**「运行脚本」**（此前应用里没有任何一处能
  "允许"——这是这个仓库第八次栽在「能力在、入口没接」上）。点过的文件记住，
  设置 → 编辑器里可以改成「一律运行」。**默认不变**：老用户升级后 HTML 仍然不跑脚本。
- 沙箱给 `allow-scripts`、**仍然不给 `allow-same-origin`**：页面在不透明来源里跑，
  摸不到应用的 DOM、localStorage、Tauri IPC。两个绝不一起给。
- 代价是不透明来源里 `localStorage` 一碰就抛，而台账类工具全靠它。所以在页面自己的
  脚本之前注入一段 shim 顶替 localStorage / sessionStorage / cookie，每次写入回传给
  外层，落到**同名 `.data.json`**——它是库里一个真文件，**随笔记同步**，手机上打开
  同一份工具，台账是一样的（比浏览器里"数据只在这台电脑这个浏览器"还好）。移动 HTML
  时数据文件跟着搬。
- 跨源的 iframe 读不到父页面的 blob:（headless Chrome 实测 naturalWidth = 0），
  脚本模式下相对路径图片改转 data: URL；窄屏重排的量法也搬进 iframe 里跑、结果
  postMessage 回来（外层读不到 contentDocument 了）。

### 状态栏不再压住 HTML / PDF 的最后一行

用户：「下面一行字又被挡住了。」主区放的是 HTML / PDF / 图片 / 表格 / 图谱时，
状态栏改成**贴底的一条**而不是浮层——iframe 里的页面没法像正文那样给自己留 40vh
底部空白，浮层一定压住它。同时那几样只对笔记有意义的项（插入图片 / AI / 分栏 /
反链 / 字数）在这些视图里不再显示：此前 HTML 开着时 `currentPath` 还是上一篇笔记，
状态栏照旧摆着"3,786 词"。

### 顺手

- 设置页「库里有 N 个文件」和「换文件夹时的文件数」此前恒为 0（`allPaths.length`
  取的是函数的形参个数，不是列表长度）。
- HTML 查看器窄屏重排有个时序竞态：srcdoc 还是空串时那次 load 会把"已判定"钉死，
  之后真正的页面进来就再也不量。空文档现在不算数。

验证：服务端 Go 测试新增历史 / 已删除两条端到端用例（真 SQLite + ApplyPush）；
前端 `lib/history`、`lib/htmlSandbox`、`lib/when`、同步引擎（远端删除进回收站、合并前
留快照）、`movePath`（数据文件跟着搬）都有单测；`scripts/verify-ui.mjs` 新增两段：
真实产物里点「运行脚本」→ `台账.html.data.json` 出现且 count=1 → 关掉重开 count=2，
以及敲字落盘 → `.ivyea/history/` 出现改之前那版 → 右栏「历史」能对照 → 恢复后编辑器与
磁盘都回到旧内容。139/139 通过。

---

## v0.11.23 — 2026-09-10

**五套配色主题：纸与墨，不是壁纸。**

设置 → 外观里多了「配色」一行：**素纸 / 稿纸 / 青瓷 / 月白 / 松烟**，每套深浅两版。

刻意**没有**照搬 IvyeaTranslate 那套（实拍照片 + 落花/星点动效）。那套在"看一眼就走"的
翻译工具上成立；笔记是要在这块屏幕上待一个小时写字的地方，铺照片会让正文永远压在图上，
飘动的粒子会持续偷走余光——两件事都和这个项目的定位打架（tokens.css 开头那句：
「安静的纸。界面应该在视线里消失，只剩文字」）。所以这里的主题只换**纸色、墨色和一处
强调色**：零图片、零动效、零额外 DOM，验收标准只有一条——连着看两小时不累。

- 「配色」与「主题（浅色/深色/跟随系统）」是**两个轴**：哪张纸 / 开不开灯。每套配色的
  深浅两版都给全了，切深色不会掉回默认绿。
- 实现只重定义 `--n-*` 与 `--brand*` 两组源头变量，派生变量全站自动跟随；
  选择器写成 `:root[data-palette=…]` 与 `:root[data-theme='dark'][data-palette=…]`，
  优先级天然压过 tokens.css，不依赖导入顺序。
- 老用户升级后仍是「素纸」（存下来的 appearance 里没有这个字段 → 归一化给默认值）。
- 守卫：设置里的色块与 CSS 里的真实色号逐条对齐（分叉了两边看着都对、没人发现）、
  每套必须有深浅两版、正文对比度 ≥ 7:1、分隔线不许抢戏。另外在真实产物里用 CDP
  逐套读了 computed 值，确认 5 套 × 深浅 = 10 种纸色都真的落到位。

⚠️ 顺手量出一条**存量问题**（本版未改）：默认「素纸」的次要文字 `--n-6` 在纸底上只有
3.39:1，低于小字该有的 4.5:1，整个产品的次要文字都在这一档。改它等于动所有人的默认
观感，按规矩要单独提出来单独改，所以这一版只保证**新主题不比默认更差**。

**Windows 安装包终于是自己的样子。**

用户下载到的 `Ivyea.Note_x64-setup.exe` 在资源管理器里显示的是 NSIS 自带的默认图标。
根因不是图标资源缺失——`icons/icon.ico` 一直在，且就是现在这枚叶子 logo：
`bundle.icon` 只管**应用本身**的图标，**安装器和卸载器是 nsis 那节里另外两个键**
（`installerIcon` / `uninstallerIcon`），不配就吃 NSIS 的默认图标。

顺带把安装向导的两张位图也做了（此前一直是 NSIS 的默认样式）：
顶部横幅 150×57、欢迎页竖图 164×314——**尺寸是 NSIS MUI2 写死的**，而 Tauri 的模板
没开 `MUI_HEADERIMAGE_NOSTRETCH`，尺寸不对不会报错、只会被拉伸变形，
所以生成脚本 `brand/gen_installer.py` 把尺寸断言在代码里。横幅只放叶子不放字：
MUI2 的标题文字和这张图共用一条横栏，谁左谁右由主题决定，图里再写一次品牌名两边都可能撞上。

本机打不了 Windows 包，配置按三条逐项核过：键名对着 `@tauri-apps/cli` 的
config.schema.json 查（**写错键 Tauri 是静默忽略的**，正是这次的病根）、
路径按相对 `tauri.conf.json` 解析、BMP 为 24 位无 alpha（NSIS 经典位图控件不认 alpha）。

---

## v0.11.22 — 2026-09-10

**图片开在主区、文件夹能改名、状态栏不再压住 AI 面板。**

### 图片像 Obsidian 那样在主区看

用户原话：「图片查看为什么不直接在侧边栏右侧的窗口自适应尺寸查看？就像 obsidian
这样」。此前点开库里的图片是一层盖住整个应用的黑色蒙层——侧栏、右栏、状态栏全被
盖掉，看一眼就得先关掉才能接着干别的。

现在图片和 PDF / `.base` / HTML / 图谱一样，是主区里"当前在看什么"的一种：
**默认适应窗口**（大图整张收进来，小图不放大），带缩放按钮、`Ctrl+滚轮`、双击在
适应窗口与 100% 之间切换，以及「用系统应用打开」。侧栏还在，随手就能点下一个文件。
手机端保留原来的全屏蒙层——小屏上主区本来就是整个屏幕。

### 文件夹重命名

侧栏右键文件夹此前有"新建 / 移动到 / 复制路径 / 删除"，唯独没有改名，想换个名字
只能新建一个再把里面的东西一件件拖过去。现在桌面右键、手机长按都有「重命名…」：
整棵子树跟着换前缀，正开着的那篇笔记、标签栏、最近打开、分栏、文件夹折叠状态都跟着
走。撞上同名文件夹**不会**悄悄改成"归档-2"——那不是用户打进去的名字，当场说明重名。

### 状态栏不再盖住 AI 面板的按钮

用户：「右下角有个按钮被盖住了」。状态栏是浮在正文上的一层（这是 v0.11.2 特意做的），
而它以整个编辑区为定位基准——AI 结果面板一开，它正好压在面板底栏上，「应用」只露出
半截。现在编辑区那一块单独包成 `.editor-stage`：状态栏钉在它里面，AI 面板排在它外面，
浮层只会浮在正文上。

---

## v0.11.21 — 2026-09-09

**手机端 HTML 重排：上一版判错了，改成量出来的。**

用户拿 v0.11.20 又试了一次自己那份 HTML 手册——**还是那个样子**：左边目录占掉一半，
右边正文每行两三个字。

上一版的判据是 `<meta name="viewport" content="width=device-width">`：
有这条就认为作者做过手机适配、原样显示。这条判据是错的——**现在但凡由工具生成的
HTML 都会带上它**，而版式该是 1000px 两栏还是两栏。声明是**意图**，不是事实。

更麻烦的是，那种页面**连横向溢出都没有**：两栏是 flex，屏幕一窄就一起被压扁，
`scrollWidth` 和屏幕一样宽，看起来"没问题"，实际每行只剩两三个字。所以光看溢出
也判不出来。

现在改成**按原样渲染完之后量真实布局**，两条判据命中任意一条就重排：

1. 横着超出屏幕——要左右拖才能看全；
2. **最宽的那个文字块不到屏幕的六成**——就是上面那种被挤成细条的情况。

自动只判一次，之后听用户的：工具条上那颗键点过之后，不会再被自动改回去。
用例里同时放了**对照组**——一份本来就适配手机的页面，必须一个字都不动，
否则"窄屏一律重排"这种偷懒实现也能让前一条变绿。

---

## v0.11.20 — 2026-09-09

**把 AI 的入口摆到明面上。**

用户装完 v0.11.19 的第一句话是：「为什么我没有看到任何 AI 按钮呢？只有在设置里面有」。
说得对——能力上一版就做完了，可入口只有两处：顶栏「⋯」的**二级**菜单，和命令面板。
两处都得先知道它在那儿才找得到。这是这个仓库第七次栽在「能力写好了、入口没接」上。

这一版给了三个明面上的入口，各自对应一种"人此刻的手在哪儿"：

- **编辑区右键 → 第一项就是「AI 助手」**。选完一段字，手本来就在右键上，这时候
  想的十有八九就是"把这段怎么样一下"，所以它排在菜单最上面、在所有格式命令之前。
  子菜单里七个动作各带一句灰字说明——「校对 / 润色 / 精简」光看名字分不清差别，
  而它们会**直接改你的正文**，点之前就该知道它要干嘛。**需要选中文字的动作在
  没选区时是置灰的**，而不是点了才弹一句"请先选中"。
- **状态栏一颗「AI」按钮**（挨着「插入图片」「分栏」）。右键是要先知道才会去按的，
  这颗是那个真正意义上的"按钮"。
- **手机端「⋯」里多了「AI 助手」**，点进去是单独一张纸。手机没有右键，这张单子
  是唯一入口——上一版 AI 只接了桌面，在手机上等于不存在。没有把七个动作平铺进
  「⋯」：那张纸本来就有大纲 / 日记 / 重命名 / 移动 / 删除 / 设置，再塞八行就成了
  上一版刚被骂过的样子。每一行下面一句小字说明它会做什么，替换类写明
  「要先选中一段文字」——手机上没有"置灰 + 悬停提示"这一说，条件只能写在明处。
  AI 结果面板在手机上贴着底栏之上，结果一出来就看得见，不用往下滚。

规矩一条没松：只发你选中的那段、结果先给对照面板、应用后 Ctrl+Z 能退回去。
「整理排版」仍然是纯本地规则，不联网、不花钱、每次结果一样。

**AI 能做的事从 7 件变成 15 件**，按「它会对你的文件做什么」分成三段——
这不是功能分类，是**风险分类**：

- **会改你选中的字**：校对、润色、精简、更正式、整理成结构、**扩写**、
  **译成英文**、**译成中文**；
- **只多给你一段东西**（附到文末或复制走）：写摘要、起标题、**提取待办**
  （会议记录 → 勾选清单）、**生成标签**；
- **一个字都不写**：**自定义指令**、**问这篇笔记**、**问整个笔记库**。

后三条是这一版真正的新东西：

- **自定义指令**——你自己说一句「改成给客户看的口吻」「把人名换成代号」。
  内置动作再多也盖不全人的需求，这一条等于无数条。有选中就改那一段，
  没选中就把结果附到文末：「没选中还要替换」只能意味着替换整篇，这个软件不做。
- **问这篇笔记 / 问整个笔记库**——后者**先在本机检索**（倒排打分 + 按行截取
  最相关的那一段），只把最相关的最多 5 篇、约 6000 字发出去，**绝不是把库传上去**。
  面板上如实写着这次送了哪几篇、多少字。答案要求标 `[[出处]]`，
  查不到就直说查不到——笔记问答里最坏的不是答不上来，而是编一个看起来像你写过的答案。

再往前推了五件事：

- **存下来的自定义动作。** 同一句指令用第二次不该再打一遍。跑一次「自定义指令」、
  结果确实是你要的，点面板上的「存为动作」起个名——它就和内置动作**并排**出现在
  AI 菜单里。存的是**你那句原话**，不是拼好的提示词，所以以后看得懂也改得动
  （设置 → AI 里能改名、删除）。
- **答案里的 `[[出处]]` 点得回去了。** 出处不是装饰，它是你唯一能核对的凭据；
  摆成一段死文字，人还得自己再搜一遍，这条链就断在最后一步。
- **「生成标签」写进 frontmatter，不再往正文末尾丢一行井号。** 标签是元数据，
  正文里那一行会跟着导出、打印、分享一起出去。合并**只增不删**，
  而且跟着这篇笔记已有的写法走（`tags: [a, b]` 还是 `- a` 的块状）。
  ⚠️ 顺手修掉一个老 bug：`lib/tags` 里用了 `\Z`——**JS 正则没有这个东西**，
  它就是字面量 Z。于是块状 tags 写在 frontmatter **最末尾**（最常见的写法）时
  整块解析不出来，表现是"标签写了，标签面板里没有"。
- **「今天写了什么 / 本周写了什么」。** 日记写不下去的真实原因是想不起来白天动过
  什么——而这件事机器全知道（每篇 .md 的 mtime）。点它会先打开今日日记，
  再把这段时间改动过的笔记各截一段送过去，写成一段带 `[[出处]]` 的小结，
  点「应用」就落进日记。日记本身不进材料（否则今天的日记会被拿去总结今天的日记）。
- **问不到时自动扩写一次查询。** 本机检索是词法的，问「降价」找不到写着「打折」的
  那篇。真正的解法是语义向量，但那要嵌入模型、要存索引、要随笔记增量更新，
  是另一个量级——**这一版没做**。折中：只在**本机一个都没找到**时，花一次极小的调用
  让模型给一串同义词再检索一遍。门槛卡在"零命中"而不是"命中少"：已经找到相关笔记时
  再掺同义词，只会把不相关的笔记也拽进材料里——给错材料比少给材料更坏，
  而且那些笔记本来就不该发出去。

**给电脑设计的 HTML 在手机上读得了了。** 用户截图：一份左目录 + 右正文的手册，
手机上目录占掉一半、正文每行两三个字。根子是 iframe 是它自己的视口——手机浏览器
会按 980px 排版再整体缩小，而 iframe 只有屏幕那么宽，作者按 1000px 写的两栏被硬塞
进 390px。现在窄屏默认**重排**：把 flex/grid/浮动/固定定位拍平成自上而下的块，
**字号一个像素不动**（缩小只会把"读不了"换成"看不清"）。页面自己声明过
`viewport width=device-width` 的照原样显示，工具条上随时能来回切。

---

## v0.11.19 — 2026-09-09

**AI 能力上线，外加应用内看 HTML。**

先说清楚分工——用户问「直接 API 能做到这些吗」，答案是分两类：

- **语言的活交给 AI**：校对 / 润色 / 精简 / 更正式 / 整理成结构 / 写摘要 / 起标题。
  走 OpenAI 兼容接口（官方、中转、或本机 Ollama / LM Studio 都行），配置就是
  `接口地址 + Key + 模型名`三件套，设置页里有**连通性测试**。
- **排版的活交给规则**：中英文空格、标点全角半角、列表符号、标题层级、多余空行，
  以及**按内容自动选字号 / 行宽 / 行高**。这些交给模型只会更慢、更贵，而且
  **同一篇两次可能给出不同结果**——排版最忌讳的就是不稳定。这一层纯本地、
  离线可用、瞬时、每次一样，代码块 / 链接地址 / frontmatter 一律不碰。

三条铁律（每条都有真实产物级的用例守着）：

1. **默认只发送你选中的那段文字**。没选中就想校对？直接拒绝并说明——绝不默认把
   整篇笔记发出去。
2. **结果先给对照，笔记一个字不动**。左右对照能看出改了哪几行，点「应用」才写回。
3. **改动进编辑器的撤销栈**，Ctrl+Z 能把 AI 的修改退回去。

另外：

- **应用内直接看 HTML**（用户：「点开 html 文件然后跳转到浏览器」）。现在在主区
  渲染，相对路径的图片与样式表都会从库里解析出来；**脚本一行都不跑**
  （sandbox 只给 same-origin、不给 scripts），要跑脚本的留了「用浏览器打开」。
  ⚠️ 一开始用的是更严的空 sandbox，实测**整页空白**——Chromium 不给不透明来源的
  `srcdoc` 建子帧，元素、属性、尺寸全正常，只有内容不存在。

---

## v0.11.18 — 2026-09-09

- **手机端的底部弹层重做**。用户原话：「这种弹窗为什么这么多横线还长短不一，好难看」。
  数一下确实有三种长度的线：组内那条从文字处起画的缩进线、组间那条通栏线、
  大纲下面那条。根子是**让线去做分组的活**——线一旦长短不一就碎，长度统一了
  又把一张纸切成好几段。现在**一根线都不画**，分组交给留白（iOS 的 context menu、
  Obsidian 移动端菜单都是这么做的）。顺带三件：
  - 菜单有了**主语**：头部一行弱化的上下文（当前笔记名）——此前「重命名 / 移动到 /
    删除」作用在哪一篇，整张纸上没有任何交代；
  - **把手不再是假的**：按住头部下滑就能关（跟手位移，松手按距离与速度判定）。
    此前画了一条可拖的把手却只能点遮罩关，是界面在撒谎；
  - 四处弹层（菜单 / 长按操作单 / 大纲 / 重命名 / 回收站）**共用同一个外壳**，
    形状与手势只有一份实现；最后一行不再贴着屏幕底边。
- **开源安装包的私有地址闸门补严了**。原来只扫 `dist/assets/*.js` 且只认
  `*.ivyea.*` 这个形状，于是 **Rust 侧、`tauri.conf.json`（更新服务器地址就写在那儿）、
  裸 IP** 全在射程之外。更要命的是**它一直是瞎的**：压缩后的 bundle 里混着非文本
  字节，GNU grep 判定它是二进制文件，而二进制文件配 `-o` 一个字都不输出——
  往 dist 里塞一个私有域名它照样报"产物干净"。现在收成 `scripts/check-no-private-host.sh`，
  门禁与打包线共用，源码走白名单、产物按可疑形状扫，四种注入实测都能拦下。

---

## v0.11.17 — 2026-09-09

**上一版把图谱放错了地方，导出的 PDF 也只有第一页。**

- **导出的 PDF 不再只有第一页**。用户拿到的是「只有第一页有字，总页数还多出那么多」。
  把他同步到服务器上的那份捞下来数过：**78 页里 77 页内容流是 0 字节**，第 1 页
  正好是一屏的量。三个原因叠在一起，全是"拿应用外壳去打印"带来的：
  ① 打印样式里那条 `.editor-host { display:block !important }` 会把阅读模式下靠
  inline `display:none` 藏起来的 **CodeMirror 又拽回来**——它是虚拟滚动的，DOM 里
  只有视口那一屏，高度却是整篇的；② `html, body { height:100% }` 在打印里就是
  "一页纸那么高"，正文溢出后不参与分页；③ 正文到 body 之间全是 flex 容器，而
  Chromium **不会把 flex item 拆到下一页**。现在导出走的是**一份只有正文的独立
  文档**，打印时把整个应用藏掉——不再受侧栏、分栏、虚拟滚动的影响，编辑模式下直接
  Ctrl+P 也能出完整的 PDF。这条现在有真 PDF 的回归用例：打一份出来逐页数内容流。
- **手机端顶栏与底栏那两条横线删了**，底部图标再往下压（原来是 52px 行高 + 整个
  安全区，图标浮在半空；现在 46px + 安全区的一半，离屏幕底边 4px）。对着 Obsidian
  移动端改的：那两栏都不画分隔线，层次靠留白。

- **图谱开在主区**（侧边栏右边那块正文区域），不再是右边那条大纲栏，也不再是盖住
  整个窗口的新页面。上一版我把「右侧窗口」理解成了最右边的大纲栏——用户纠正：
  「是侧边栏的右侧，也就是中间空白的这里」。主区本来就是"当前在看什么"的位置，
  图谱和 PDF、`.base` 表格一样是其中一种。
- **从侧栏点文件不再越点越多标签**。对齐 Obsidian：普通单击在**当前标签里换**，
  Ctrl/⌘ 点击或中键点击才另起一个（右键菜单也补了「在新标签打开」）。此前每点一篇
  就多一个标签，点几篇之后标签栏挤到看不见名字。
- **顶栏的 `+` 改成"新标签页"**，不再是"新建文件"（用户点名）。空白标签里点一篇
  没开过的笔记就地装进去，不会再多出一个。
- **删掉的文件自己从标签栏消失**。此前 `pruneTabs` 每个库只在启动时跑一次，所以
  不管是自己删的、别处删的、还是同步拉下来的删除，标签都会一直挂着，点它才报一句
  「系统找不到指定的文件」。现在跟着每一次文件刷新对账；万一还是点到了死标签，
  也会当场把它摘掉而不是每点一次报一次。
- **手机端有回收站了**（「⋯」→ 回收站）。此前删掉的东西在手机上既看不见也恢复不了，
  而手机恰恰是最容易误删的那一端。列表复用桌面那套面板，手机上动作常显（没有 hover）。

---

## v0.11.16 — 2026-09-09

- **导出为 PDF 不再需要打印机**。用户原话：「为什么导出为 PDF 还需要链接打印机？
  我的需求是直接能转成 PDF 文件」。此前那里只有一句 `window.print()`——弹的是系统
  打印对话框，得在里面挑一个叫「Microsoft Print to PDF」的虚拟打印机。现在
  Windows 上走 WebView2 的 `PrintToPdf`：选个保存位置，直接落一个 PDF，而且是
  **矢量**的（文字能选中、能搜索、简历筛选系统能解析），不是把页面截图拼出来的。
  macOS / Linux 暂时仍走打印面板（macOS 面板自带「存储为 PDF」），并且**先判断
  支不支持再决定要不要弹保存框**，不会让人选完位置才说做不到。
- **左栏那一排按钮现在只做一件事：切换左栏显示什么**。此前「文件 / 搜索」切面板，
  而「标签 / 回收站 / 图谱」点了会盖一张对话框上来——同一排图标、两种行为
  （用户原话：「很乱，视觉上体验很差」）。现在**标签和回收站都是左栏的面板**：
  标签是一列可筛选、右侧对齐引用次数的行（不再是一堵大小不一的胶囊墙）；
  回收站每行显示的是**原来的名字和原来的目录**（此前摆的是
  `2026-09-09T…-目录__文件.md` 这种编码过的文件名），恢复/彻底删除收进行尾的
  图标、悬停才出现，并且多了「清空」——十几个文件不必确认十几次。
- **图谱回到右栏**，不再是一个要退出去才能回到正文的整屏页面（用户：「图谱为什么
  是一个新的页面？不应该也是在右侧窗口吗」）。它成了右栏「大纲 / 反向链接」旁边
  的第三个标签，默认看当前这篇的局部图；要铺开看全库，面板里还有一颗「全屏打开」。
- **新增「今日日记」入口**，就在最左侧那一排的下面（手机端放在「⋯」里）。
  这个能力从 v0.7.1 就写好了（`lib/daily` + 模板占位符），但此前**只有命令面板
  能到**——这是这个仓库第 N 次「能力写好了、入口没接」。
- **侧栏头部更紧、也不再半边空着**。库名那一行现在铺满整个侧栏、下拉箭头顶到右
  边缘（它本来就是"点开换库"的下拉行）；上下留白收紧。加上 v0.11.14 已经把
  「新建笔记 / 新建文件夹 / 排序 / 全部折叠」搬进顶栏，侧栏顶端到文件树第一行
  由约 90px 收到 54px。

---

## v0.11.15 — 2026-09-09

**编辑器里的四处硬伤，外加文件夹终于能删。**

- **大标题现在点得到、方向键也走得到**。用户原话：「我用键盘的方向键无法移动到
  大标题，用鼠标也无法点击到大标题，上方向键经常跳到很上面」。真因在 Live Preview
  的样式上：标题行用了 `line-height` 与 `margin`，而 CodeMirror 的高度模型量的是
  `getBoundingClientRect()`——外边距不在盒子里，行高它也不认，于是它算出来的坐标
  从第一个标题起就开始偏，标题越多偏得越远：点在标题上落到别的行、上键一按整段跳过。
  改成只用 `padding`、不设 `line-height`（拿真实产物逐条抹样式验出来的）。
- **无序列表画成圆点**（Obsidian 同款），不再原样露着一个 `-`。只换渲染，
  文档里那个 `-` 一个字节没动。
- **链接看得出是链接**：品牌色之外补一条淡下划线。此前只给颜色，对比 Obsidian 时
  用户的原话是「也没有自动识别链接」。
- **新建笔记后改标题不再弹一句"重命名失败"**。回车提交后焦点交给正文，紧跟着的
  `blur` 会拿**上一拍的草稿**再提交一次，第二次用的是已经不存在的旧路径——于是
  「已重命名」和「重命名失败：系统找不到指定的文件」一起弹出来。文件其实一直是
  对的，错的只是那句提示。
- **文件夹右键可以删除了**。此前只有文件那一支有删除，文件夹只能一篇篇删完还剩
  个空壳。整夹的文件进回收站，可原路恢复；文件夹外的一律不动。
- **`.base` 表格现在数得全**。用户原话：「个人空间统计不完全啊，图片，pdf，
  个人空间本身的 .base 文件都没有在个人空间里面体现」。真因是喂给它的是**正文
  索引**（只含 `.md`）——而 Obsidian Bases 的数据源是"库里的文件"。现在图片、
  PDF、别的 `.base` 一并进表，`file.name / ext / folder / mtime / size` 照常可筛；
  点表里的图片会开图片预览、PDF 会开阅读器，不再拿文本通道去读一张 PNG。
- **手机端底部最右边那颗键说清楚自己是干什么的**。用户原话：「点了之后看不到
  反应啊，也没个动效，也没有反应」。三处都缺：没登录时它调的是一个第一行就
  return 的同步函数（现在改成把登录页叫出来）；转圈的 class 挂了但样式表里**从来
  没有过那条规则**（现在真的会转）；同步完只有"有变更"才提示（现在手点的那一次
  一定有回执：`已同步 ↑n ↓n` / `已是最新` / `离线，联网后自动同步`）。

---

## v0.11.14 — 2026-09-09

**手机端四处 + 桌面端标签栏。**

- **顶栏不再跟着正文滑走**。上一版把滚动交给主区是对的（标题该跟着走），错在
  顶栏当时还渲染在主区**里面**——滚的是连顶栏一起的那一层，于是想点左上角的
  侧栏按钮得先滑回最顶（用户原话）。现在顶栏是应用的直接子元素，第一层 UI 钉住，
  标题与正文在它下面滚。
- **连不上服务器不再动不动弹一段红字**。自动同步在启动、每次切回前台、每 60 秒
  各跑一次，而手机上"刚解锁 / 切回前台 / VPN 正在重连"正好落在这些时刻；此前每
  撞上一次，就把「拉取失败：连不上服务器（Failed to fetch）」外加三条排查提示整段
  贴在正文上方，直到下次成功才消失。那三条是给"服务端装错了"准备的，对一次网络
  抖动毫无用处。现在自动同步撞上网络错误只留一句「离线」，联网后自己就没了；
  **手动点同步仍然给完整原因**——那时人就是来看原因的。登录过期、库没接上这类
  "重试不会好"的错误一个都没被压下去。
- **手机端点图片终于能看到图**。全屏图片层此前只挂在桌面那棵组件树上，手机点了
  以后照样解析、照样 setState，然后什么都不渲染。
- **底部弹窗是一整张纸**。此前只有分组卡片有底色，卡片之间的缝里露着模糊的正文，
  看起来不像同一个东西（用户：「整个弹窗没有背景色，所以看起来还是有空隙感」）。
  现在整张纸有底色、上两角圆、遮罩压暗，分组退回成纸上的分区。
- **桌面端标签和它底下那一页连在一起**（参照浏览器与 Obsidian）。标签条从内容区
  的左边界起画，当前标签与页面同底色、底边相接、不画外框。腾出来的"侧栏正上方"
  那一格，装的是从侧栏搬上来的四颗常用按钮（新建笔记 / 新建文件夹 / 排序 / 全部
  折叠）——**搬**而不是复制，侧栏里那一行同时删掉；侧栏一收，这一格连按钮一起收走。

---

## v0.11.13 — 2026-09-08

**手机端三处 + 一个被它们牵出来的真 bug。**

- **打开一篇笔记不再把它改名、也不再写盘**。编辑器的 `updateListener` 对**任何**文档
  变化都上报，包括"把文件内容灌进编辑器"那一次——于是光是打开一篇带 H1 的笔记，
  就会防抖写盘一次、并按 H1 把文件改名（用户：「文件本来就是这个名字，还非要再命名
  一次」）。现在给那次程序化变更打标注，监听器跳过它；人敲的字照常。
- **手机端标题不再冻结在顶部**。原因不是哪里写了 sticky，是布局：标题在编辑器外面，
  而编辑器自己内部滚。现在手机端把滚动交给外层，编辑器与阅读区随内容长——
  标题跟着正文一起走（Obsidian 移动端就是这样）。桌面端不变。
- **大纲弹层改成贴底的圆角卡片**（此前是直角、还浮在半空），与底部菜单同一套形状：
  把手、圆角、分组卡片、背景虚化。
- **底部四个键按"手机上真正高频"重排：搜索 / 新建 / 大纲 / 同步**。
  「格式」不再占一个常驻位——它只在光标落在正文里时有用，改成**编辑器一拿到焦点
  就自动出现**；省下的位置给了大纲（长文没它只能一路划）和同步（这个产品的核心动作，
  之前要点开「⋯」才找得到）。

---

## v0.11.12 — 2026-09-08

**补发 v0.11.11。** 那一版的 Windows 安装包没打出来——不是产物有问题（593 个测试在
Windows 上全过），是 v0.11.11 新加的那条「真服务端端到端」用例**清理得不够干净**：
它用 `go run` 起服务端，而 `go run` 会另起一个子进程跑编出来的程序，Windows 上
`kill` 杀掉的是外层 wrapper，真正的服务端还占着 SQLite 文件的句柄，于是删临时目录时
撞 `EBUSY`，把整个 Windows 打包任务判红。

- 改成**先 `go build` 成可执行文件再直接 spawn**：`kill` 打的就是服务端本身，顺带每次少编一遍。
- 等进程真的退出再删目录；删不掉就重试几次，最后放弃——**清理失败绝不能把测试判红**。
- v0.11.11 的功能与修复全部包含在本版中（那一版的 macOS / Linux / 安卓包是好的）。

---

## v0.11.11 — 2026-09-08

**修 v0.11.10 捅的同步事故，外加用户报的四条界面问题。**

- **同步全面停摆已修（v0.11.10 的回归）**。三条叠在一起：
  1. `io.list()` 故意保留 `.ivyea/`（应用自己的索引缓存）与 `.trash/`（本机回收站），
     v0.11.10 起"非 .md 一律同步"把它们也推上了云 —— 手机端那条红条
     「`.ivyea/cache/content.json` 上传失败」就是它。现在这两个目录一个字节都不上传，
     云端已有的也不会落回本地。
  2. `getBlob` / `putBlob` **绕开了 401 自动刷新**（其它请求一直都有）。access token
     只有 15 分钟，放一会儿之后附件一律 401，而且永远不会自己好。现在两个都会刷新重试。
  3. **一个文件失败会跳过整段拉取** —— 于是"桌面端改的笔记手机端看不到"。现在只有
     整条链路断了（未接入 / 登录过期 / 推送请求失败）才停，单个文件传不上去照说明、
     不连累其余。
- **任务列表能打勾了**。点复选框时插件在 `update()` 里同步 `dispatch`，CodeMirror 抛异常
  → **整个 Live Preview 插件被停用**，所有装饰当场消失 —— 现象就是"点一下就变成中括号"。
  现在改到微任务里派发；复选框一律渲染不再退回源码；连行首那个 `-` 一起收进复选框；
  任务行做悬挂缩进，折行的文字对齐第一行。
- **清单不再整页发灰**。编辑器高亮里 `t.list` 用了次要文字色，而 Lezer 的 `t.list` 盖的是
  **整个列表项**而非行首符号 —— 一篇满是清单的笔记，正文全成了 `#8b8880`。现在与正文同色。
- **打开 `.base` 之后点别的笔记会跳过去了**。主区是三选一，而表格只在按「关闭」时才收，
  于是从侧栏点任何一篇都像"点了没反应"（其实笔记已经打开，被表格盖着）。
- **正文里有颜色了**（品牌绿）。上色的位置照 Obsidian 挑，只给"不是正文"的那些东西：
  链接、引用左线、行内代码、`==高亮==`、任务勾、列表符号、代码块的语言名、表头线与分隔线。
  正文、加粗、标题一律保持墨色——满屏绿字比满屏黑字更难读。编辑态与阅读态**同一套颜色**，
  切视图不会变色；深色主题自动换成提亮过的那支绿。
- **标签页回到顶栏**。v0.10.7 删掉的是"单独占一整行、空荡荡的那条栏"，不是标签本身；
  现在照 Obsidian 把标签放进顶栏（顺便把这条本来有点空的栏填满）：点标签切换、
  ✕ 或中键关闭、`+` 新建，关掉当前标签会落到相邻那一篇（不是掉回空白页），
  重开还在。标签之间的缝隙与右侧余白仍然可以拖窗口。
- **字数统计口径与 Obsidian 对齐**。同一篇文档 Obsidian 报 6,158 词 / 25,251 字符，
  这边只有 5,503 / 15,448 —— 拿那篇 34KB 的真文档逐项比对，差在三处：字符数**去掉了空白**、
  **整段剔掉了代码块**、数字里的小数点被当成断词。现在字符 = 原文长度（含空白与语法），
  代码块计入，`18.06` 算一个词。改完实测：字符 25,251（完全一致），词 6,157（差 1）。
- **窄窗口下点开右栏能看见大纲了**。`@media (max-width:1080px)` 把 `.right-panel`
  直接 `display:none`：窗口窄一点（用户那台约 960px）时，点竖轨上的展开按钮，
  组件从"竖轨"切成"面板"，而面板正好被藏掉 —— 整条右栏凭空消失，连回去的入口都没了。
  现在窄窗口改成浮在正文之上。
- **不再显示两个标题**。文件名与正文首个 H1 说的是同一件事时（差别常常只在 `/` 这种
  文件名非法字符上，如 `AI 高效智能 / 单端…` 与清洗后的 `AI 高效智能 单端…`），
  不再在正文上方重复顶一行内联标题。两者真的不同时照常都显示。
- **重开回到退出前那篇笔记**。按库分别记住，文件被删/改名就安静跳过，只还原 Markdown。
- **新增真服务端端到端验证**（`sync.e2e.test.ts`）：起一个真的 Go 服务端、在真磁盘目录上
  跑真同步引擎，逐条钉死上面三个事故。上面三条修复各自撤掉之后，对应用例都会红。

---

## v0.11.10 — 2026-09-08

- **PDF、图片和 `.base` 现在会同步到手机**。此前同步链路里只有 `.md`：扫描本地文件那一步
  就把其它扩展名全过滤掉了，所以"电脑上明明有 PDF、手机上没有"不是同步坏了，是它从来
  没被列进要同步的东西里。协议这层本来就是内容寻址的 blob（收发的是字节），补的是客户端
  这一段。附件不做三方合并：两端都改同一个文件时，服务端那份落回原路径，本地那份留成
  同扩展名的冲突副本（`a.conflict-….pdf`），不静默覆盖任何一端。单文件上限 50MB，
  超了会明确说，而不是悄悄不传。同步状态面板现在也统计附件。
- **能打开 `.base` 了**（Obsidian Bases）。点开不再是"交给 Obsidian"，而是本地渲染成表格：
  解析那份 YAML，按 `filters` 筛库里的笔记，按 `order` 显示列，支持 `and/or/not` 嵌套、
  比较运算、`hasTag / hasLink / inFolder / hasProperty / contains / startsWith / isEmpty`
  这些方法，以及多视图切换、点表头排序、`groupBy`、`limit`。**看不懂的表达式会明说**
  （底部列出被忽略的那几条），不会悄悄少筛一条让你对着一张多出几十行的表发愣。
  暂不支持 `formulas` / `summaries` / 日期函数。
- **代码块终于看得出是代码块**。编辑态此前没有围栏解析，` ``` ` 就是三个字面反引号，
  中间的代码按正文排版、还会被行内规则接着装饰；现在整块有底色、等宽字、首尾各一条边，
  块内不再套用 Markdown 行内语法。阅读态做成卡片：语言名 + 一键复制。
- **frontmatter 不再被当成正文**。`---` 那段以前会渲染成「分隔线 + 二级标题」
  （满屏 `status: doing`），现在是文档顶部的只读属性区，与 Obsidian 一致。
- **手机端**：抽屉右侧由直角改成圆角并加投影（浮在正文之上，而不是被切开一刀）；
  底部菜单的分组卡片加大圆角与投影、行高提到 52px、分隔线从文字处起画不再横穿图标栏，
  背景轻微虚化。

---

## v0.11.9 — 2026-09-08

- **修复手机端换不了笔记库**（「桌面端绑定的文件夹始终无法同步到手机」的真因）。
  手机端库名旁边那个 ∨ 点开只有「新建笔记库」和「标签」——库列表**从来没有渲染过**：
  `MobileView` 收着一个 `vaultSelector` 属性，声明了、一次都没用。
  于是电脑推 A 库、手机停在 B 库，而手机换不过去。现在那张菜单第一组就是全部笔记库，
  当前的打勾，点一下就切。
- **`.base` / `.canvas` 打不开时先交给 Obsidian**。它们是 Obsidian 自己的格式，
  Windows 上不会有任何程序关联，但 Obsidian 注册了 `obsidian://` 协议 ——
  装了 Obsidian 的机器现在直接把文件甩过去；没装才退回到「在文件夹中定位」。
- **自定义标题不会再被改回去**。`titleSync` 是「正文首个 H1 → 文件名」的单向同步，
  改完标题后正文里的 H1 还是旧的，下一次编辑就把文件名拽了回去。
  现在**显式改标题时把正文那个 H1 一起带上**（只在开着 titleSync 时动正文）。
- **收起侧边栏的按钮对齐了**：中心从 x=33 挪到 x=21，和它正下方 ribbon 那列图标
  同一条竖线；此前它既没对上 ribbon、又骑在 ribbon 与侧栏的分界线上。
- **侧边栏收起/展开有动画了**（180ms 宽度过渡）。为此侧栏改成始终挂在树上——
  卸载了就没有可过渡的东西；收完再 `visibility: hidden`，免得里面的按钮还能被 Tab 选中。
  跟随系统的「减少动态效果」设置。
- **浮层不再那么透**：设置面板、命令面板这类浮层的底色从 78% 提到 94%。
  下面压着满屏正文时，78% 模糊完仍是一片灰花，标签和说明读起来费劲。
- 发版流水线：打包矩阵改成串行（`max-parallel: 1`）。四个任务并行往同一个
  `latest.json` 上写，后写的会覆盖先写的、倒霉的那个还会撞 `Not Found` ——
  v0.11.8 发版时就丢了 macOS 两个平台的更新入口（安装包本身没事，已补回）。

## v0.11.8 — 2026-09-08

- **修复手机端「拉取失败：refresh token 无效或已过期」这条永远消不掉的红条**。
  服务端把 refresh token 轮换掉之后，重试多少次都是同一个 401，而应用既不说明要重新登录、
  也没有能点的入口。现在同步报告单独标出「登录态过期」：**停掉自动重试**，
  桌面端状态栏变成「登录已过期」、手机端红条上直接带一颗「重新登录」按钮，点了就能重登；
  **账号不会被清掉**（清了的话正在用的云端库连同它绑定的磁盘目录会整个从界面上消失）。
- **修复登录时把新令牌扔掉**。登录流程里两个临时客户端的「令牌轮换回调」写的是
  `() => undefined`：这中间只要发生一次 401 自动刷新，服务端就会把旧 refresh token 删掉、
  发一个新的，而新的被当场丢弃 —— 存进本地的那个从此作废。上面那条红条的成因之一。
- **系统里没有能打开这个文件的程序时，退回到「在文件夹中定位」**。Obsidian 的 `.base`
  在 Windows 上没有任何关联程序，此前只会甩一句「电脑上没有可用应用」然后什么也不发生；
  现在自动定位到它所在的文件夹，至少能右键「打开方式」自己挑一个。
- **`.gitignore` 这类点开头的文件也隐藏了**（v0.11.7 只隐藏了点开头的目录）。
  自己的 `.keep` 占位文件除外——去掉它「新建文件夹」就等于点了没反应。
- **已完成的任务项不再灰得读不动**。`- [x]` 的正文此前被压到 60% 透明度，
  实测约等于 #6f6e6a，在纸色背景上已经掉到"次要文字"的亮度；删除线本身已经把
  「这条划掉了」说清楚了，颜色不必再帮腔。正文墨色一直是 #2b2a26，没有变过。
- **顶栏补上两个真正高频的按钮**（刻意不凑数、也不重复界面上已有的）：
  左边**收起/展开侧边栏**（Ctrl+\，和 Obsidian 同一个键位），右边**「⋯」笔记动作菜单**
  （导出为 PDF / 重命名 / 移动到 / 复制路径 / 在文件夹中显示 / 删除）。
  阅读-编辑切换和分栏在别处已经有按钮，菜单里不再出现第二次。
- **侧边栏可以收起了**，连同它右边那条拖宽手柄一起收掉（只藏侧栏、留着手柄会剩下
  "一条能拖的缝"），状态记在本地，重启还是你上次的样子。
- **新增「导出为 PDF」**。走系统打印对话框里的「另存为 PDF」，排版与阅读视图一致、
  中文字体不用另外内嵌。导出前会先切到阅读视图——编辑器是虚拟滚动的，
  直接打印只会得到一份**只有一屏内容**的 PDF。
- **窗口投影不再生硬**。上一版是 `0 10px 30px`，向下扩散 25px、四周 15px，
  全都超出那圈 10px 透明留白被齐齐切掉，看到的是一圈深带而不是影子。现在收进留白里。
- **窗口缩放热区挪到看得见的边上**。系统给无边框窗口的缩放边框落在窗口最外一像素，
  离卡片边还有 10px 透明留白，瞄着阴影拖是拖不动的；现在自己铺了一圈热区。

## v0.11.7 — 2026-09-08

- **修复「登录成功、同步永远失败：vault 不存在或不属于你」**。
  把本地库接到云端这段协调**只长在登录那一刻**，而且整段裹在一个 `catch {}` 里。
  v0.11.5 的 CORS 故障让它抛掉之后，账号存下来了、库却只剩一个负数 id 的本地库——
  之后每一轮自动同步都拿这个 id 去 `POST /sync/push`，服务端一律回 403，
  **而且不重新登录就永远不会再协调第二次**（在生产库里核实过：报障账号名下 0 个 vault、0 个 device）。
  现在这段逻辑独立成 `lib/vaultLink.ts`，同步引擎一拿到 403 或发现当前还是本地库就自己接回来，
  打开应用时也补一次；用户不需要知道「退出登录再登一次」这种内部知识。
- **接入云端时不再把你选的文件夹换成应用内部存储**。此前无论本地库有没有绑磁盘目录，
  一律新建一个 `opfs://` 的云端库再把笔记复制进去——用户选的 `E:\obsidian` 就此失联。
  现在绑了真实文件夹（含安卓 SAF）的库**直接继承这份绑定，一个文件都不搬**；
  只有应用内部存储的库才需要搬家（存储目录是 `vault-<id>`，换 id 必须跟着走）。
- **登录不再抹掉其它本地笔记库**。v0.10.2 起可以有多个本地库，但登录时只把 `-1` 并进去，
  `-2`、`-3` 直接从 state 里消失（磁盘上的笔记还在，界面上再也找不回来）。
- **换到另一个服务端库时清零同步进度**。版本号只在同一个服务端库里有意义，
  带着旧 `versions` 过去会让推送判定成「没变化」——笔记一篇都传不上去，界面却显示同步成功。
- **修复「无法打开：Not allowed to open path …」**。v0.11.6 只补了 ACL 的一半：
  `opener` 有一套**独立于 `fs:scope`** 的路径作用域，而 `opener:allow-open-path` 的定义就是
  "without any pre-configured scope"——不给作用域，任何路径都打不开。
  现在给它 `**`，并把 `requireLiteralLeadingDot` 关掉（Unix 上默认为真，
  `.obsidian/` 这类目录里的文件会被 `**` 漏掉）。
- **修复 Windows 上「凡是要新建目录的写入全废」**（v0.11.4 起）。`join` 那次改成反斜杠时，
  取父目录的 `parentOf` 还只找 `/` —— Windows 上父目录恒为空串，`mkdir(dir,{recursive:true})`
  整句被跳过。受害的是：第一次删除笔记（要写 `.trash/`）、新建文件夹（写 `子目录/.keep`）、
  往新子目录里粘图片、以及**同步拉取远端新目录下的笔记**。
- **修复图片 / PDF 删不掉**。删除是「先搬进回收站再删原文件」，而搬运走的是**文本**读写：
  非 UTF-8 的文件在第一步就抛，于是「删不掉」；重名递增又写死成只认 `.md`，
  非 .md 撞名时那个 `while` 会原地打转。现在按字节搬（笔记同样无损），序号加在扩展名之前。
  恢复那一侧同样改成按字节，否则恢复出来的图片是被有损解码过的废文件。
- **修复外部改动要「重新加载」才看得到**。文件监听从 v0.7.5 就有，但它只刷文件列表和索引、
  **从不碰编辑区**——在 Obsidian 里改了正开着的那篇，这边一直是旧内容。
  现在监听触发时重读当前笔记（分栏的第二篇也一起），并补一条**回到前台就对账**的兜底
  （安卓 SAF 没有系统级监听，桌面上 `watch` 也可能悄悄起不来）。
  本地还有没落盘的编辑时不覆盖，不会吃掉刚敲的字。
- **`.git` / `.obsidian` 这类点目录不再进文件树**（和 Obsidian 一致）。此前整棵 `.git`
  都被扫进来，侧栏被 `objects/…` 淹掉，`listMeta` 还要逐个 `stat`——一个正常大小的
  git 仓库就能让每次刷新卡好几秒。自己的 `.trash`/`.ivyea` 照常保留。
- **保存失败不再只写 console**：笔记应用里「保存悄悄失败」是最坏的一种失败，现在会弹提示。
- **状态栏不再在「一次都没同步过」时写「已同步」**。同步报告为 null 时旧判定也算「没出错」，
  于是「登录着、库没接上、引擎根本没跑完过」会显示一片祥和，而另一台设备什么也收不到。
  这种状态现在显示「待同步」。
- 服务端版本号跟到 0.11.7（服务端逻辑本身没有变化）。

## v0.11.6 — 2026-09-07

- **修复"登录成功但同步失败"**。v0.11.5 的 CORS 把 `Allow-Headers` 写死成 `Authorization, Content-Type`，而同步的每条请求都带 `X-Device-Id`——浏览器逐条比对，缺一个整个请求就发不出去。登录不带那个头，所以看起来"能连上"。现在**回显浏览器请求的头**，客户端将来加新头也不会再挂。
- **修复点开非笔记文件报 `Command plugin:opener|open_path not allowed by ACL`**。`opener:default` 只授予 `open-url` 与 `reveal-item-in-dir`，**从不包含 `open-path`**——这意味着"交给系统应用打开"（安卓看 PDF、桌面看 docx/zip）一直是坏的。
- **修复窗口与桌面糊在一起**。v0.11.4 为去掉系统那圈 1px 方框设了 `shadow: false`，代价是投影也没了。现在四周留一圈透明带、自己画投影（和 Ivyea Translate 同一套做法），最大化时收掉。
- **收紧文档标题的留白**：距顶栏 32px → 12px，行高 1.3 → 1.25，正文上内边距 24px → 8px。原先顶栏加留白一共 70px 全是空的。
- 「测试连接」现在能显示服务端版本了：`/healthz` 补上 `X-Ivyea-Version`，并通过 `Access-Control-Expose-Headers` 暴露给跨域的客户端。
- 服务端版本号从 0.8.10 跟到 0.11.6。

## v0.11.5 — 2026-09-07

- **修复桌面端与安卓端连不上远程服务器**（客户端报 `Failed to fetch` / 「域名解析失败或服务未启动」）。
  服务端**从来没有输出过任何 CORS 响应头**，而桌面/安卓的 WebView 源是 `http://tauri.localhost`，
  对服务器而言是跨域——浏览器把响应整个拦掉，客户端只能看到一句 `Failed to fetch`。
  这条路一直没被发现，是因为历次端到端验证都跑在网页版（`/app/` 由服务器自己托管）上，**同源，恰好绕开了 CORS**。
  现在服务端回显请求方 Origin 并正确响应 `OPTIONS` 预检（此前预检直接 405，带 `Authorization` 的请求根本发不出去）。
  API 是 Bearer 令牌鉴权、不用 Cookie，因此不声明 `Allow-Credentials`。
- **`fetch` 失败时的提示不再误导**：此前只说「域名解析失败或服务未启动」，把跨域这种最常见的原因藏了起来。现在三种可能都说出来，并给一句可执行的自查（用浏览器直接打开那个地址）。

> ⚠️ 升级说明：这条修复在**服务端**。桌面端/手机端升到 v0.11.5 之后，**还需要把自建服务器也升级到 v0.11.5**，跨域才会被放行。

## v0.11.4 — 2026-09-07

- **修复粘贴图片被文件系统作用域拒绝**（`forbidden path: …`）。绑定文件夹时的作用域是对话框插件顺手给的，而它**只放行库根那一层**；再叠上「路径不存在就不做 canonicalize」，结果是**已有笔记读写全正常、唯独在子目录里新建附件被拒**。现在每次激活笔记库都由应用自己递归放行该目录。
- **顶栏照 Obsidian 的布局重做**：中间是面包屑（目录 / 文件名），右端是阅读/编辑切换与窗口按钮，**整条都能拖窗口**。v0.11.3 把它整条删掉、按钮浮在右上角，代价是窗口没地方拖了。
- 阅读/编辑模式提升为 App 持有的受控状态，顶栏开关与编辑器共用同一份。
- 状态栏不再重复显示笔记路径（面包屑已经在顶栏说了）。
- Windows 上拼接库内路径改用反斜杠，不再拼出 `E:\a\b/c/d.png` 这种混合分隔符。

## v0.11.3 — 2026-09-07

- **顶部横栏整条去掉。** 三颗窗口按钮改为浮在右上角，侧栏与正文从窗口第 0 像素开始，不再有任何保留行；窗口拖动靠按钮左侧的透明区与侧栏顶部那行库名。当前笔记路径回到状态栏。
- **粘贴图片：每一条失败路径现在都会说话。** 此前有三条静默 `return` 加一个静默 `catch`（没有回调 / 不是图片 / 没有当前笔记 / 落盘抛异常），四种情况用户看到的都是"什么都没发生"。
- **粘贴图片改挂在编辑器自己的 DOM 上**（此前只有外层的 React 委托监听），外层保留作兜底并去重；取文件时 `items` 与 `files` 两个来源都看——不同 WebView 只填其中一个。
- **新增「插入 → 剪贴板里的图片」**：绕开粘贴事件直接读系统剪贴板，粘贴键在某些 WebView 上拿不到位图时仍然能用。
- 拖入图片也改用同一套取文件逻辑。

## v0.11.2 — 2026-09-07

- **修复 PDF 只显示一小截**：页面容器被纵向 flex 布局压扁（实测容器 205px / 内容 1102px），且被一条多余的 `max-width` 夹窄。
- **修复关闭 PDF 预览会弹出错误页**：销毁调用打在了错误的对象上（`destroy()` 属于加载任务，不属于文档对象）。
- **修复 PDF 页码永远显示最后一页**，改由滚动位置计算。
- **修复标题行里的行内语法从不渲染**：加粗、斜体、行内代码、链接、图片在 `#` 标题行上一直被跳过——粘贴图片时光标恰好停在标题行，就表现为「粘贴了但看不见」。
- **状态栏改成右下角浮层**（对齐 Obsidian，不再占用一整行），新增「N 条反向链接」；打开 PDF 时不再显示无意义的字数统计。

## v0.11.1 — 2026-09-07

- **文件树显示库里全部文件**（对齐 Obsidian）。PDF 此前被单独钉在侧栏底部一个扁平分组里，在多层目录的库中几乎找不到；现在它和图片、附件都待在各自的文件夹里，非 Markdown 文件带类型角标。点开按类型分流：笔记进编辑器、PDF 进阅读器、图片全屏查看、其余交给系统应用。
- **Windows 窗口去掉系统画的 1px 方框**（`shadow: false`），圆角不再被框住。
- **自绘标题栏改成应用头部**：品牌标记 + 当前笔记路径，底色与侧栏一致；状态栏不再重复显示路径。

## v0.11.0 — 2026-09-07

- **Windows 无边框圆角窗口**：去掉系统标题栏，自绘最小化/最大化/关闭，最大化时自动收成直角。仅 Windows 生效，macOS 保留原生红绿灯、Linux 保留原生边框。
- **编辑区完整右键菜单**：新增链接 / 新增外部链接、「文本格式」「段落设置」「插入」三个二级菜单、剪切/复制/粘贴/以纯文本形式粘贴/全选；右键点在链接或图片上时给出对应动作。菜单支持图标、分隔线、置灰、快捷键提示与键盘操作。
- **编辑态直接显示图片**（此前图片只在阅读模式渲染），文件缺失时给出明确占位。
- **新增 `~~删除线~~` 与 `==高亮==`**，编辑态与阅读态表现一致。
- **修复斜体与行内代码的样式互换**（潜伏六个版本的正则分组错位）。
- **应用内 PDF 阅读器**（pdf.js）：按需渲染、页码导航、缩放与适应宽度，三端行为一致。此前依赖 WebView 自带阅读器，Linux 与安卓上根本没有。
- **图谱重做**：占满窗口、持续力导向布局、缩放/平移/拖拽节点、搜索过滤与悬停聚焦、局部图可调跳数；普通 Markdown 链接现在也算边（此前只认 `[[双链]]`）。
- **同步连接诊断**：一次性摆出「服务端在不在跑 / Windows 防火墙放没放行 / 手机该填哪个地址 / 是否同一网段」四道关，能修的直接给按钮。安装包会尝试写入防火墙放行规则，应用内也能补。
- 设置里新增「当前同步方式」一览与「换一台服务器」入口。

## v0.10.7 — 2026-09-03

- 删掉编辑区上方的标签栏，正文直接顶到窗口边。
- 状态栏补上「插入图片」入口（此前该能力只能靠粘贴或拖入触发）。
- 附件默认改为与笔记同一文件夹，可在设置里改回统一目录。
- 图标全平台改为圆角样式。

## v0.10.6 — 2026-09-03

- 同步与首次启动的一次全面排查：修正设置里的错误提示、补上开启同步后的返回入口、同步失败不再显示成「已同步」。
- 局域网发现改用应答包的来源地址，避免连到 Docker/WSL/虚拟机网卡。
- 退出应用时清理内置服务端进程（此前会残留并占住端口）。

## v0.10.5 — 2026-09-03

- **内置同步服务端**：设置里一个开关，这台电脑即成为同步服务器，自动建账号并登录。
- 安卓支持选择系统目录（SAF）。

## v0.10.4 — 2026-09-02

- 移除公开构建里预置的同步服务器域名。自托管软件不该有「官方服务器」。

## v0.10.3 — 2026-09-02

- **6 位配对码**：手机上只输一串数字即可连上电脑，不用填地址和密码。
- 「开启同步」「添加设备」从命令面板移到设置里。
- 修复服务端两个会让配对码当场作废的缺陷（重试计数恒真、反向代理后限速桶只有一个）。

## v0.10.2 — 2026-09-02

- **修复正文不换行**（编辑器缺少软换行扩展，长段落只能横向滚动）。
- **修复侧栏拖不动文件**（Tauri 的系统级拖放会禁掉 WebView 内的 HTML5 拖放）。
- **修复点链接会把整个应用导航走**（白屏后只能重启）。
- 删掉移动端上下重复的按钮；修复移动端长按操作单从未渲染。
- 安卓改用固定签名密钥（此前每发一版换一把，用户每次更新都被迫卸载重装）。
- 未登录也能新建笔记库；设置里新增「存储位置」。

## v0.10.1 — 2026-08-30

- 更换全平台 logo。
- **内联标题**：文件名即标题，改标题就是改文件名。
- 修复打开笔记时标题行顶着一个 `#`；修复表格右对齐失效；文件树补上层级引导线。

## v0.10.0 — 2026-08-30

- 视觉语言与信息架构重做，桌面与移动端一起对标 Obsidian：侧栏收敛为「库名 + 一行图标 + 文件树」，同步状态下沉到状态栏，移动端改为 5 格常驻导航 + 可展开格式条，右栏改为「大纲 / 反向链接」标签页。

## v0.9.2 — 2026-08-29

- **同步状态面板**：说得出「现在还差什么」，而不是只有一个「同步」按钮。
- 修复「同步成功、上传 0、笔记永远上不去」（藏了七个版本的静默失败）。

## v0.9.1 — 2026-08-29

- 端到端补课：修复三个 P0，「登录 → 同步 → 另一端拉到」这条核心链路此前从未真正跑通。

## v0.9.0 — 2026-08-29

- 修复一个从 v0.2.0 潜伏至今的登录崩溃。

## v0.8.x — 2026-08-29

- **P2 高频体验**：侧栏与右栏可拖拽调宽、编辑区左右分栏、文内查找替换、最近打开与快速切换、侧栏搜索带上下文预览、设置页集中（外观/编辑器/同步/快捷键/关于）、移动可撤销。
- **实时预览扩展**：表格、callout、脚注、分隔线。
- **移动端补课**：新建文件夹入口、全库正文搜索、标签面板。
- **服务端 MCP endpoint** 与 `notes_write`：笔记库可直接作为 Agent 的读写终端。

## v0.7.x — 2026-08-26 ~ 08-29

- **检索引擎重写**：倒排索引 + BM25，中文二元组分词，索引快照持久化；此前是全库小写化后 `includes` 全扫。
- **修复全库正文索引形同虚设**：旧实现只在打开命令面板时建一次，导致反向链接在移动端从未显示过。
- **设计系统落地**：token / 排版 / 平面层次三层拆分，编辑态与阅读态共用同一套文字规则。
- **侧栏拖拽移动**文件与文件夹；**文件监听**，外部编辑器改同一目录能感知。
- 移动端交互对标 Obsidian：长按操作单、可折叠文件树、边缘手势、阅读模式活预览、图片 lightbox。
- 图谱视图、标签面板、`[[双链]]` 与补全、粘贴/拖入图片。

## v0.3.x — 2026-08-25 ~ 08-26

- **免登录本地模式**：不注册也能完整使用，登录改为按需。
- 修复 Windows 上「除文本输入外按钮全点不动」的五个根因。
- 编辑器统一升级为 CodeMirror 6（移动端此前是裸 textarea），阅读模式、排序、图片与 PDF 支持。
- 安卓 APK 由 CI 自动打包并签名。

## v0.1.0 ~ v0.2.0 — 2026-08-24

- 同步协议与一致性场景（C1~C8）、Go 服务端（auth / vault / sync / blob / WebSocket）、部署编排。
- 桌面端核心：CodeMirror 编辑器、笔记库管理、增量同步引擎（3-way diff3 合并、删改复活、冲突副本、墓碑去重）。
