# 变更记录

版本号遵循 `主.次.修`。日期为发布日。
完整的安装包见 [Releases](https://github.com/Hector-xue/IvyeaNote/releases)。

---

## v0.11.16 — 2026-09-09

- **导出为 PDF 不再需要打印机**。用户原话：「为什么导出为 PDF 还需要链接打印机？
  我的需求是直接能转成 PDF 文件」。此前那里只有一句 `window.print()`——弹的是系统
  打印对话框，得在里面挑一个叫「Microsoft Print to PDF」的虚拟打印机。现在
  Windows 上走 WebView2 的 `PrintToPdf`：选个保存位置，直接落一个 PDF，而且是
  **矢量**的（文字能选中、能搜索、简历筛选系统能解析），不是把页面截图拼出来的。
  macOS / Linux 暂时仍走打印面板（macOS 面板自带「存储为 PDF」），并且**先判断
  支不支持再决定要不要弹保存框**，不会让人选完位置才说做不到。
- **左栏那一排按钮现在只做一件事：切换左栏显示什么**。此前「文件 / 搜索」切面板，
  而「标签 / 回收站 / 图谱」点了会盖一张对话框上来——同一排图标、两种行为
  （用户原话：「很乱，视觉上体验很差」）。现在**标签和回收站都是左栏的面板**：
  标签是一列可筛选、右侧对齐引用次数的行（不再是一堵大小不一的胶囊墙）；
  回收站每行显示的是**原来的名字和原来的目录**（此前摆的是
  `2026-09-09T…-目录__文件.md` 这种编码过的文件名），恢复/彻底删除收进行尾的
  图标、悬停才出现，并且多了「清空」——十几个文件不必确认十几次。
- **图谱回到右栏**，不再是一个要退出去才能回到正文的整屏页面（用户：「图谱为什么
  是一个新的页面？不应该也是在右侧窗口吗」）。它成了右栏「大纲 / 反向链接」旁边
  的第三个标签，默认看当前这篇的局部图；要铺开看全库，面板里还有一颗「全屏打开」。
- **新增「今日日记」入口**，就在最左侧那一排的下面（手机端放在「⋯」里）。
  这个能力从 v0.7.1 就写好了（`lib/daily` + 模板占位符），但此前**只有命令面板
  能到**——这是这个仓库第 N 次「能力写好了、入口没接」。
- **侧栏头部更紧、也不再半边空着**。库名那一行现在铺满整个侧栏、下拉箭头顶到右
  边缘（它本来就是"点开换库"的下拉行）；上下留白收紧。加上 v0.11.14 已经把
  「新建笔记 / 新建文件夹 / 排序 / 全部折叠」搬进顶栏，侧栏顶端到文件树第一行
  由约 90px 收到 54px。

---

## v0.11.15 — 2026-09-09

**编辑器里的四处硬伤，外加文件夹终于能删。**

- **大标题现在点得到、方向键也走得到**。用户原话：「我用键盘的方向键无法移动到
  大标题，用鼠标也无法点击到大标题，上方向键经常跳到很上面」。真因在 Live Preview
  的样式上：标题行用了 `line-height` 与 `margin`，而 CodeMirror 的高度模型量的是
  `getBoundingClientRect()`——外边距不在盒子里，行高它也不认，于是它算出来的坐标
  从第一个标题起就开始偏，标题越多偏得越远：点在标题上落到别的行、上键一按整段跳过。
  改成只用 `padding`、不设 `line-height`（拿真实产物逐条抹样式验出来的）。
- **无序列表画成圆点**（Obsidian 同款），不再原样露着一个 `-`。只换渲染，
  文档里那个 `-` 一个字节没动。
- **链接看得出是链接**：品牌色之外补一条淡下划线。此前只给颜色，对比 Obsidian 时
  用户的原话是「也没有自动识别链接」。
- **新建笔记后改标题不再弹一句"重命名失败"**。回车提交后焦点交给正文，紧跟着的
  `blur` 会拿**上一拍的草稿**再提交一次，第二次用的是已经不存在的旧路径——于是
  「已重命名」和「重命名失败：系统找不到指定的文件」一起弹出来。文件其实一直是
  对的，错的只是那句提示。
- **文件夹右键可以删除了**。此前只有文件那一支有删除，文件夹只能一篇篇删完还剩
  个空壳。整夹的文件进回收站，可原路恢复；文件夹外的一律不动。
- **`.base` 表格现在数得全**。用户原话：「个人空间统计不完全啊，图片，pdf，
  个人空间本身的 .base 文件都没有在个人空间里面体现」。真因是喂给它的是**正文
  索引**（只含 `.md`）——而 Obsidian Bases 的数据源是"库里的文件"。现在图片、
  PDF、别的 `.base` 一并进表，`file.name / ext / folder / mtime / size` 照常可筛；
  点表里的图片会开图片预览、PDF 会开阅读器，不再拿文本通道去读一张 PNG。
- **手机端底部最右边那颗键说清楚自己是干什么的**。用户原话：「点了之后看不到
  反应啊，也没个动效，也没有反应」。三处都缺：没登录时它调的是一个第一行就
  return 的同步函数（现在改成把登录页叫出来）；转圈的 class 挂了但样式表里**从来
  没有过那条规则**（现在真的会转）；同步完只有"有变更"才提示（现在手点的那一次
  一定有回执：`已同步 ↑n ↓n` / `已是最新` / `离线，联网后自动同步`）。

---

## v0.11.14 — 2026-09-09

**手机端四处 + 桌面端标签栏。**

- **顶栏不再跟着正文滑走**。上一版把滚动交给主区是对的（标题该跟着走），错在
  顶栏当时还渲染在主区**里面**——滚的是连顶栏一起的那一层，于是想点左上角的
  侧栏按钮得先滑回最顶（用户原话）。现在顶栏是应用的直接子元素，第一层 UI 钉住，
  标题与正文在它下面滚。
- **连不上服务器不再动不动弹一段红字**。自动同步在启动、每次切回前台、每 60 秒
  各跑一次，而手机上"刚解锁 / 切回前台 / VPN 正在重连"正好落在这些时刻；此前每
  撞上一次，就把「拉取失败：连不上服务器（Failed to fetch）」外加三条排查提示整段
  贴在正文上方，直到下次成功才消失。那三条是给"服务端装错了"准备的，对一次网络
  抖动毫无用处。现在自动同步撞上网络错误只留一句「离线」，联网后自己就没了；
  **手动点同步仍然给完整原因**——那时人就是来看原因的。登录过期、库没接上这类
  "重试不会好"的错误一个都没被压下去。
- **手机端点图片终于能看到图**。全屏图片层此前只挂在桌面那棵组件树上，手机点了
  以后照样解析、照样 setState，然后什么都不渲染。
- **底部弹窗是一整张纸**。此前只有分组卡片有底色，卡片之间的缝里露着模糊的正文，
  看起来不像同一个东西（用户：「整个弹窗没有背景色，所以看起来还是有空隙感」）。
  现在整张纸有底色、上两角圆、遮罩压暗，分组退回成纸上的分区。
- **桌面端标签和它底下那一页连在一起**（参照浏览器与 Obsidian）。标签条从内容区
  的左边界起画，当前标签与页面同底色、底边相接、不画外框。腾出来的"侧栏正上方"
  那一格，装的是从侧栏搬上来的四颗常用按钮（新建笔记 / 新建文件夹 / 排序 / 全部
  折叠）——**搬**而不是复制，侧栏里那一行同时删掉；侧栏一收，这一格连按钮一起收走。

---

## v0.11.13 — 2026-09-08

**手机端三处 + 一个被它们牵出来的真 bug。**

- **打开一篇笔记不再把它改名、也不再写盘**。编辑器的 `updateListener` 对**任何**文档
  变化都上报，包括"把文件内容灌进编辑器"那一次——于是光是打开一篇带 H1 的笔记，
  就会防抖写盘一次、并按 H1 把文件改名（用户：「文件本来就是这个名字，还非要再命名
  一次」）。现在给那次程序化变更打标注，监听器跳过它；人敲的字照常。
- **手机端标题不再冻结在顶部**。原因不是哪里写了 sticky，是布局：标题在编辑器外面，
  而编辑器自己内部滚。现在手机端把滚动交给外层，编辑器与阅读区随内容长——
  标题跟着正文一起走（Obsidian 移动端就是这样）。桌面端不变。
- **大纲弹层改成贴底的圆角卡片**（此前是直角、还浮在半空），与底部菜单同一套形状：
  把手、圆角、分组卡片、背景虚化。
- **底部四个键按"手机上真正高频"重排：搜索 / 新建 / 大纲 / 同步**。
  「格式」不再占一个常驻位——它只在光标落在正文里时有用，改成**编辑器一拿到焦点
  就自动出现**；省下的位置给了大纲（长文没它只能一路划）和同步（这个产品的核心动作，
  之前要点开「⋯」才找得到）。

---

## v0.11.12 — 2026-09-08

**补发 v0.11.11。** 那一版的 Windows 安装包没打出来——不是产物有问题（593 个测试在
Windows 上全过），是 v0.11.11 新加的那条「真服务端端到端」用例**清理得不够干净**：
它用 `go run` 起服务端，而 `go run` 会另起一个子进程跑编出来的程序，Windows 上
`kill` 杀掉的是外层 wrapper，真正的服务端还占着 SQLite 文件的句柄，于是删临时目录时
撞 `EBUSY`，把整个 Windows 打包任务判红。

- 改成**先 `go build` 成可执行文件再直接 spawn**：`kill` 打的就是服务端本身，顺带每次少编一遍。
- 等进程真的退出再删目录；删不掉就重试几次，最后放弃——**清理失败绝不能把测试判红**。
- v0.11.11 的功能与修复全部包含在本版中（那一版的 macOS / Linux / 安卓包是好的）。

---

## v0.11.11 — 2026-09-08

**修 v0.11.10 捅的同步事故，外加用户报的四条界面问题。**

- **同步全面停摆已修（v0.11.10 的回归）**。三条叠在一起：
  1. `io.list()` 故意保留 `.ivyea/`（应用自己的索引缓存）与 `.trash/`（本机回收站），
     v0.11.10 起"非 .md 一律同步"把它们也推上了云 —— 手机端那条红条
     「`.ivyea/cache/content.json` 上传失败」就是它。现在这两个目录一个字节都不上传，
     云端已有的也不会落回本地。
  2. `getBlob` / `putBlob` **绕开了 401 自动刷新**（其它请求一直都有）。access token
     只有 15 分钟，放一会儿之后附件一律 401，而且永远不会自己好。现在两个都会刷新重试。
  3. **一个文件失败会跳过整段拉取** —— 于是"桌面端改的笔记手机端看不到"。现在只有
     整条链路断了（未接入 / 登录过期 / 推送请求失败）才停，单个文件传不上去照说明、
     不连累其余。
- **任务列表能打勾了**。点复选框时插件在 `update()` 里同步 `dispatch`，CodeMirror 抛异常
  → **整个 Live Preview 插件被停用**，所有装饰当场消失 —— 现象就是"点一下就变成中括号"。
  现在改到微任务里派发；复选框一律渲染不再退回源码；连行首那个 `-` 一起收进复选框；
  任务行做悬挂缩进，折行的文字对齐第一行。
- **清单不再整页发灰**。编辑器高亮里 `t.list` 用了次要文字色，而 Lezer 的 `t.list` 盖的是
  **整个列表项**而非行首符号 —— 一篇满是清单的笔记，正文全成了 `#8b8880`。现在与正文同色。
- **打开 `.base` 之后点别的笔记会跳过去了**。主区是三选一，而表格只在按「关闭」时才收，
  于是从侧栏点任何一篇都像"点了没反应"（其实笔记已经打开，被表格盖着）。
- **正文里有颜色了**（品牌绿）。上色的位置照 Obsidian 挑，只给"不是正文"的那些东西：
  链接、引用左线、行内代码、`==高亮==`、任务勾、列表符号、代码块的语言名、表头线与分隔线。
  正文、加粗、标题一律保持墨色——满屏绿字比满屏黑字更难读。编辑态与阅读态**同一套颜色**，
  切视图不会变色；深色主题自动换成提亮过的那支绿。
- **标签页回到顶栏**。v0.10.7 删掉的是"单独占一整行、空荡荡的那条栏"，不是标签本身；
  现在照 Obsidian 把标签放进顶栏（顺便把这条本来有点空的栏填满）：点标签切换、
  ✕ 或中键关闭、`+` 新建，关掉当前标签会落到相邻那一篇（不是掉回空白页），
  重开还在。标签之间的缝隙与右侧余白仍然可以拖窗口。
- **字数统计口径与 Obsidian 对齐**。同一篇文档 Obsidian 报 6,158 词 / 25,251 字符，
  这边只有 5,503 / 15,448 —— 拿那篇 34KB 的真文档逐项比对，差在三处：字符数**去掉了空白**、
  **整段剔掉了代码块**、数字里的小数点被当成断词。现在字符 = 原文长度（含空白与语法），
  代码块计入，`18.06` 算一个词。改完实测：字符 25,251（完全一致），词 6,157（差 1）。
- **窄窗口下点开右栏能看见大纲了**。`@media (max-width:1080px)` 把 `.right-panel`
  直接 `display:none`：窗口窄一点（用户那台约 960px）时，点竖轨上的展开按钮，
  组件从"竖轨"切成"面板"，而面板正好被藏掉 —— 整条右栏凭空消失，连回去的入口都没了。
  现在窄窗口改成浮在正文之上。
- **不再显示两个标题**。文件名与正文首个 H1 说的是同一件事时（差别常常只在 `/` 这种
  文件名非法字符上，如 `AI 高效智能 / 单端…` 与清洗后的 `AI 高效智能 单端…`），
  不再在正文上方重复顶一行内联标题。两者真的不同时照常都显示。
- **重开回到退出前那篇笔记**。按库分别记住，文件被删/改名就安静跳过，只还原 Markdown。
- **新增真服务端端到端验证**（`sync.e2e.test.ts`）：起一个真的 Go 服务端、在真磁盘目录上
  跑真同步引擎，逐条钉死上面三个事故。上面三条修复各自撤掉之后，对应用例都会红。

---

## v0.11.10 — 2026-09-08

- **PDF、图片和 `.base` 现在会同步到手机**。此前同步链路里只有 `.md`：扫描本地文件那一步
  就把其它扩展名全过滤掉了，所以"电脑上明明有 PDF、手机上没有"不是同步坏了，是它从来
  没被列进要同步的东西里。协议这层本来就是内容寻址的 blob（收发的是字节），补的是客户端
  这一段。附件不做三方合并：两端都改同一个文件时，服务端那份落回原路径，本地那份留成
  同扩展名的冲突副本（`a.conflict-….pdf`），不静默覆盖任何一端。单文件上限 50MB，
  超了会明确说，而不是悄悄不传。同步状态面板现在也统计附件。
- **能打开 `.base` 了**（Obsidian Bases）。点开不再是"交给 Obsidian"，而是本地渲染成表格：
  解析那份 YAML，按 `filters` 筛库里的笔记，按 `order` 显示列，支持 `and/or/not` 嵌套、
  比较运算、`hasTag / hasLink / inFolder / hasProperty / contains / startsWith / isEmpty`
  这些方法，以及多视图切换、点表头排序、`groupBy`、`limit`。**看不懂的表达式会明说**
  （底部列出被忽略的那几条），不会悄悄少筛一条让你对着一张多出几十行的表发愣。
  暂不支持 `formulas` / `summaries` / 日期函数。
- **代码块终于看得出是代码块**。编辑态此前没有围栏解析，` ``` ` 就是三个字面反引号，
  中间的代码按正文排版、还会被行内规则接着装饰；现在整块有底色、等宽字、首尾各一条边，
  块内不再套用 Markdown 行内语法。阅读态做成卡片：语言名 + 一键复制。
- **frontmatter 不再被当成正文**。`---` 那段以前会渲染成「分隔线 + 二级标题」
  （满屏 `status: doing`），现在是文档顶部的只读属性区，与 Obsidian 一致。
- **手机端**：抽屉右侧由直角改成圆角并加投影（浮在正文之上，而不是被切开一刀）；
  底部菜单的分组卡片加大圆角与投影、行高提到 52px、分隔线从文字处起画不再横穿图标栏，
  背景轻微虚化。

---

## v0.11.9 — 2026-09-08

- **修复手机端换不了笔记库**（「桌面端绑定的文件夹始终无法同步到手机」的真因）。
  手机端库名旁边那个 ∨ 点开只有「新建笔记库」和「标签」——库列表**从来没有渲染过**：
  `MobileView` 收着一个 `vaultSelector` 属性，声明了、一次都没用。
  于是电脑推 A 库、手机停在 B 库，而手机换不过去。现在那张菜单第一组就是全部笔记库，
  当前的打勾，点一下就切。
- **`.base` / `.canvas` 打不开时先交给 Obsidian**。它们是 Obsidian 自己的格式，
  Windows 上不会有任何程序关联，但 Obsidian 注册了 `obsidian://` 协议 ——
  装了 Obsidian 的机器现在直接把文件甩过去；没装才退回到「在文件夹中定位」。
- **自定义标题不会再被改回去**。`titleSync` 是「正文首个 H1 → 文件名」的单向同步，
  改完标题后正文里的 H1 还是旧的，下一次编辑就把文件名拽了回去。
  现在**显式改标题时把正文那个 H1 一起带上**（只在开着 titleSync 时动正文）。
- **收起侧边栏的按钮对齐了**：中心从 x=33 挪到 x=21，和它正下方 ribbon 那列图标
  同一条竖线；此前它既没对上 ribbon、又骑在 ribbon 与侧栏的分界线上。
- **侧边栏收起/展开有动画了**（180ms 宽度过渡）。为此侧栏改成始终挂在树上——
  卸载了就没有可过渡的东西；收完再 `visibility: hidden`，免得里面的按钮还能被 Tab 选中。
  跟随系统的「减少动态效果」设置。
- **浮层不再那么透**：设置面板、命令面板这类浮层的底色从 78% 提到 94%。
  下面压着满屏正文时，78% 模糊完仍是一片灰花，标签和说明读起来费劲。
- 发版流水线：打包矩阵改成串行（`max-parallel: 1`）。四个任务并行往同一个
  `latest.json` 上写，后写的会覆盖先写的、倒霉的那个还会撞 `Not Found` ——
  v0.11.8 发版时就丢了 macOS 两个平台的更新入口（安装包本身没事，已补回）。

## v0.11.8 — 2026-09-08

- **修复手机端「拉取失败：refresh token 无效或已过期」这条永远消不掉的红条**。
  服务端把 refresh token 轮换掉之后，重试多少次都是同一个 401，而应用既不说明要重新登录、
  也没有能点的入口。现在同步报告单独标出「登录态过期」：**停掉自动重试**，
  桌面端状态栏变成「登录已过期」、手机端红条上直接带一颗「重新登录」按钮，点了就能重登；
  **账号不会被清掉**（清了的话正在用的云端库连同它绑定的磁盘目录会整个从界面上消失）。
- **修复登录时把新令牌扔掉**。登录流程里两个临时客户端的「令牌轮换回调」写的是
  `() => undefined`：这中间只要发生一次 401 自动刷新，服务端就会把旧 refresh token 删掉、
  发一个新的，而新的被当场丢弃 —— 存进本地的那个从此作废。上面那条红条的成因之一。
- **系统里没有能打开这个文件的程序时，退回到「在文件夹中定位」**。Obsidian 的 `.base`
  在 Windows 上没有任何关联程序，此前只会甩一句「电脑上没有可用应用」然后什么也不发生；
  现在自动定位到它所在的文件夹，至少能右键「打开方式」自己挑一个。
- **`.gitignore` 这类点开头的文件也隐藏了**（v0.11.7 只隐藏了点开头的目录）。
  自己的 `.keep` 占位文件除外——去掉它「新建文件夹」就等于点了没反应。
- **已完成的任务项不再灰得读不动**。`- [x]` 的正文此前被压到 60% 透明度，
  实测约等于 #6f6e6a，在纸色背景上已经掉到"次要文字"的亮度；删除线本身已经把
  「这条划掉了」说清楚了，颜色不必再帮腔。正文墨色一直是 #2b2a26，没有变过。
- **顶栏补上两个真正高频的按钮**（刻意不凑数、也不重复界面上已有的）：
  左边**收起/展开侧边栏**（Ctrl+\，和 Obsidian 同一个键位），右边**「⋯」笔记动作菜单**
  （导出为 PDF / 重命名 / 移动到 / 复制路径 / 在文件夹中显示 / 删除）。
  阅读-编辑切换和分栏在别处已经有按钮，菜单里不再出现第二次。
- **侧边栏可以收起了**，连同它右边那条拖宽手柄一起收掉（只藏侧栏、留着手柄会剩下
  "一条能拖的缝"），状态记在本地，重启还是你上次的样子。
- **新增「导出为 PDF」**。走系统打印对话框里的「另存为 PDF」，排版与阅读视图一致、
  中文字体不用另外内嵌。导出前会先切到阅读视图——编辑器是虚拟滚动的，
  直接打印只会得到一份**只有一屏内容**的 PDF。
- **窗口投影不再生硬**。上一版是 `0 10px 30px`，向下扩散 25px、四周 15px，
  全都超出那圈 10px 透明留白被齐齐切掉，看到的是一圈深带而不是影子。现在收进留白里。
- **窗口缩放热区挪到看得见的边上**。系统给无边框窗口的缩放边框落在窗口最外一像素，
  离卡片边还有 10px 透明留白，瞄着阴影拖是拖不动的；现在自己铺了一圈热区。

## v0.11.7 — 2026-09-08

- **修复「登录成功、同步永远失败：vault 不存在或不属于你」**。
  把本地库接到云端这段协调**只长在登录那一刻**，而且整段裹在一个 `catch {}` 里。
  v0.11.5 的 CORS 故障让它抛掉之后，账号存下来了、库却只剩一个负数 id 的本地库——
  之后每一轮自动同步都拿这个 id 去 `POST /sync/push`，服务端一律回 403，
  **而且不重新登录就永远不会再协调第二次**（在生产库里核实过：报障账号名下 0 个 vault、0 个 device）。
  现在这段逻辑独立成 `lib/vaultLink.ts`，同步引擎一拿到 403 或发现当前还是本地库就自己接回来，
  打开应用时也补一次；用户不需要知道「退出登录再登一次」这种内部知识。
- **接入云端时不再把你选的文件夹换成应用内部存储**。此前无论本地库有没有绑磁盘目录，
  一律新建一个 `opfs://` 的云端库再把笔记复制进去——用户选的 `E:\obsidian` 就此失联。
  现在绑了真实文件夹（含安卓 SAF）的库**直接继承这份绑定，一个文件都不搬**；
  只有应用内部存储的库才需要搬家（存储目录是 `vault-<id>`，换 id 必须跟着走）。
- **登录不再抹掉其它本地笔记库**。v0.10.2 起可以有多个本地库，但登录时只把 `-1` 并进去，
  `-2`、`-3` 直接从 state 里消失（磁盘上的笔记还在，界面上再也找不回来）。
- **换到另一个服务端库时清零同步进度**。版本号只在同一个服务端库里有意义，
  带着旧 `versions` 过去会让推送判定成「没变化」——笔记一篇都传不上去，界面却显示同步成功。
- **修复「无法打开：Not allowed to open path …」**。v0.11.6 只补了 ACL 的一半：
  `opener` 有一套**独立于 `fs:scope`** 的路径作用域，而 `opener:allow-open-path` 的定义就是
  "without any pre-configured scope"——不给作用域，任何路径都打不开。
  现在给它 `**`，并把 `requireLiteralLeadingDot` 关掉（Unix 上默认为真，
  `.obsidian/` 这类目录里的文件会被 `**` 漏掉）。
- **修复 Windows 上「凡是要新建目录的写入全废」**（v0.11.4 起）。`join` 那次改成反斜杠时，
  取父目录的 `parentOf` 还只找 `/` —— Windows 上父目录恒为空串，`mkdir(dir,{recursive:true})`
  整句被跳过。受害的是：第一次删除笔记（要写 `.trash/`）、新建文件夹（写 `子目录/.keep`）、
  往新子目录里粘图片、以及**同步拉取远端新目录下的笔记**。
- **修复图片 / PDF 删不掉**。删除是「先搬进回收站再删原文件」，而搬运走的是**文本**读写：
  非 UTF-8 的文件在第一步就抛，于是「删不掉」；重名递增又写死成只认 `.md`，
  非 .md 撞名时那个 `while` 会原地打转。现在按字节搬（笔记同样无损），序号加在扩展名之前。
  恢复那一侧同样改成按字节，否则恢复出来的图片是被有损解码过的废文件。
- **修复外部改动要「重新加载」才看得到**。文件监听从 v0.7.5 就有，但它只刷文件列表和索引、
  **从不碰编辑区**——在 Obsidian 里改了正开着的那篇，这边一直是旧内容。
  现在监听触发时重读当前笔记（分栏的第二篇也一起），并补一条**回到前台就对账**的兜底
  （安卓 SAF 没有系统级监听，桌面上 `watch` 也可能悄悄起不来）。
  本地还有没落盘的编辑时不覆盖，不会吃掉刚敲的字。
- **`.git` / `.obsidian` 这类点目录不再进文件树**（和 Obsidian 一致）。此前整棵 `.git`
  都被扫进来，侧栏被 `objects/…` 淹掉，`listMeta` 还要逐个 `stat`——一个正常大小的
  git 仓库就能让每次刷新卡好几秒。自己的 `.trash`/`.ivyea` 照常保留。
- **保存失败不再只写 console**：笔记应用里「保存悄悄失败」是最坏的一种失败，现在会弹提示。
- **状态栏不再在「一次都没同步过」时写「已同步」**。同步报告为 null 时旧判定也算「没出错」，
  于是「登录着、库没接上、引擎根本没跑完过」会显示一片祥和，而另一台设备什么也收不到。
  这种状态现在显示「待同步」。
- 服务端版本号跟到 0.11.7（服务端逻辑本身没有变化）。

## v0.11.6 — 2026-09-07

- **修复"登录成功但同步失败"**。v0.11.5 的 CORS 把 `Allow-Headers` 写死成 `Authorization, Content-Type`，而同步的每条请求都带 `X-Device-Id`——浏览器逐条比对，缺一个整个请求就发不出去。登录不带那个头，所以看起来"能连上"。现在**回显浏览器请求的头**，客户端将来加新头也不会再挂。
- **修复点开非笔记文件报 `Command plugin:opener|open_path not allowed by ACL`**。`opener:default` 只授予 `open-url` 与 `reveal-item-in-dir`，**从不包含 `open-path`**——这意味着"交给系统应用打开"（安卓看 PDF、桌面看 docx/zip）一直是坏的。
- **修复窗口与桌面糊在一起**。v0.11.4 为去掉系统那圈 1px 方框设了 `shadow: false`，代价是投影也没了。现在四周留一圈透明带、自己画投影（和 Ivyea Translate 同一套做法），最大化时收掉。
- **收紧文档标题的留白**：距顶栏 32px → 12px，行高 1.3 → 1.25，正文上内边距 24px → 8px。原先顶栏加留白一共 70px 全是空的。
- 「测试连接」现在能显示服务端版本了：`/healthz` 补上 `X-Ivyea-Version`，并通过 `Access-Control-Expose-Headers` 暴露给跨域的客户端。
- 服务端版本号从 0.8.10 跟到 0.11.6。

## v0.11.5 — 2026-09-07

- **修复桌面端与安卓端连不上远程服务器**（客户端报 `Failed to fetch` / 「域名解析失败或服务未启动」）。
  服务端**从来没有输出过任何 CORS 响应头**，而桌面/安卓的 WebView 源是 `http://tauri.localhost`，
  对服务器而言是跨域——浏览器把响应整个拦掉，客户端只能看到一句 `Failed to fetch`。
  这条路一直没被发现，是因为历次端到端验证都跑在网页版（`/app/` 由服务器自己托管）上，**同源，恰好绕开了 CORS**。
  现在服务端回显请求方 Origin 并正确响应 `OPTIONS` 预检（此前预检直接 405，带 `Authorization` 的请求根本发不出去）。
  API 是 Bearer 令牌鉴权、不用 Cookie，因此不声明 `Allow-Credentials`。
- **`fetch` 失败时的提示不再误导**：此前只说「域名解析失败或服务未启动」，把跨域这种最常见的原因藏了起来。现在三种可能都说出来，并给一句可执行的自查（用浏览器直接打开那个地址）。

> ⚠️ 升级说明：这条修复在**服务端**。桌面端/手机端升到 v0.11.5 之后，**还需要把自建服务器也升级到 v0.11.5**，跨域才会被放行。

## v0.11.4 — 2026-09-07

- **修复粘贴图片被文件系统作用域拒绝**（`forbidden path: …`）。绑定文件夹时的作用域是对话框插件顺手给的，而它**只放行库根那一层**；再叠上「路径不存在就不做 canonicalize」，结果是**已有笔记读写全正常、唯独在子目录里新建附件被拒**。现在每次激活笔记库都由应用自己递归放行该目录。
- **顶栏照 Obsidian 的布局重做**：中间是面包屑（目录 / 文件名），右端是阅读/编辑切换与窗口按钮，**整条都能拖窗口**。v0.11.3 把它整条删掉、按钮浮在右上角，代价是窗口没地方拖了。
- 阅读/编辑模式提升为 App 持有的受控状态，顶栏开关与编辑器共用同一份。
- 状态栏不再重复显示笔记路径（面包屑已经在顶栏说了）。
- Windows 上拼接库内路径改用反斜杠，不再拼出 `E:\a\b/c/d.png` 这种混合分隔符。

## v0.11.3 — 2026-09-07

- **顶部横栏整条去掉。** 三颗窗口按钮改为浮在右上角，侧栏与正文从窗口第 0 像素开始，不再有任何保留行；窗口拖动靠按钮左侧的透明区与侧栏顶部那行库名。当前笔记路径回到状态栏。
- **粘贴图片：每一条失败路径现在都会说话。** 此前有三条静默 `return` 加一个静默 `catch`（没有回调 / 不是图片 / 没有当前笔记 / 落盘抛异常），四种情况用户看到的都是"什么都没发生"。
- **粘贴图片改挂在编辑器自己的 DOM 上**（此前只有外层的 React 委托监听），外层保留作兜底并去重；取文件时 `items` 与 `files` 两个来源都看——不同 WebView 只填其中一个。
- **新增「插入 → 剪贴板里的图片」**：绕开粘贴事件直接读系统剪贴板，粘贴键在某些 WebView 上拿不到位图时仍然能用。
- 拖入图片也改用同一套取文件逻辑。

## v0.11.2 — 2026-09-07

- **修复 PDF 只显示一小截**：页面容器被纵向 flex 布局压扁（实测容器 205px / 内容 1102px），且被一条多余的 `max-width` 夹窄。
- **修复关闭 PDF 预览会弹出错误页**：销毁调用打在了错误的对象上（`destroy()` 属于加载任务，不属于文档对象）。
- **修复 PDF 页码永远显示最后一页**，改由滚动位置计算。
- **修复标题行里的行内语法从不渲染**：加粗、斜体、行内代码、链接、图片在 `#` 标题行上一直被跳过——粘贴图片时光标恰好停在标题行，就表现为「粘贴了但看不见」。
- **状态栏改成右下角浮层**（对齐 Obsidian，不再占用一整行），新增「N 条反向链接」；打开 PDF 时不再显示无意义的字数统计。

## v0.11.1 — 2026-09-07

- **文件树显示库里全部文件**（对齐 Obsidian）。PDF 此前被单独钉在侧栏底部一个扁平分组里，在多层目录的库中几乎找不到；现在它和图片、附件都待在各自的文件夹里，非 Markdown 文件带类型角标。点开按类型分流：笔记进编辑器、PDF 进阅读器、图片全屏查看、其余交给系统应用。
- **Windows 窗口去掉系统画的 1px 方框**（`shadow: false`），圆角不再被框住。
- **自绘标题栏改成应用头部**：品牌标记 + 当前笔记路径，底色与侧栏一致；状态栏不再重复显示路径。

## v0.11.0 — 2026-09-07

- **Windows 无边框圆角窗口**：去掉系统标题栏，自绘最小化/最大化/关闭，最大化时自动收成直角。仅 Windows 生效，macOS 保留原生红绿灯、Linux 保留原生边框。
- **编辑区完整右键菜单**：新增链接 / 新增外部链接、「文本格式」「段落设置」「插入」三个二级菜单、剪切/复制/粘贴/以纯文本形式粘贴/全选；右键点在链接或图片上时给出对应动作。菜单支持图标、分隔线、置灰、快捷键提示与键盘操作。
- **编辑态直接显示图片**（此前图片只在阅读模式渲染），文件缺失时给出明确占位。
- **新增 `~~删除线~~` 与 `==高亮==`**，编辑态与阅读态表现一致。
- **修复斜体与行内代码的样式互换**（潜伏六个版本的正则分组错位）。
- **应用内 PDF 阅读器**（pdf.js）：按需渲染、页码导航、缩放与适应宽度，三端行为一致。此前依赖 WebView 自带阅读器，Linux 与安卓上根本没有。
- **图谱重做**：占满窗口、持续力导向布局、缩放/平移/拖拽节点、搜索过滤与悬停聚焦、局部图可调跳数；普通 Markdown 链接现在也算边（此前只认 `[[双链]]`）。
- **同步连接诊断**：一次性摆出「服务端在不在跑 / Windows 防火墙放没放行 / 手机该填哪个地址 / 是否同一网段」四道关，能修的直接给按钮。安装包会尝试写入防火墙放行规则，应用内也能补。
- 设置里新增「当前同步方式」一览与「换一台服务器」入口。

## v0.10.7 — 2026-09-03

- 删掉编辑区上方的标签栏，正文直接顶到窗口边。
- 状态栏补上「插入图片」入口（此前该能力只能靠粘贴或拖入触发）。
- 附件默认改为与笔记同一文件夹，可在设置里改回统一目录。
- 图标全平台改为圆角样式。

## v0.10.6 — 2026-09-03

- 同步与首次启动的一次全面排查：修正设置里的错误提示、补上开启同步后的返回入口、同步失败不再显示成「已同步」。
- 局域网发现改用应答包的来源地址，避免连到 Docker/WSL/虚拟机网卡。
- 退出应用时清理内置服务端进程（此前会残留并占住端口）。

## v0.10.5 — 2026-09-03

- **内置同步服务端**：设置里一个开关，这台电脑即成为同步服务器，自动建账号并登录。
- 安卓支持选择系统目录（SAF）。

## v0.10.4 — 2026-09-02

- 移除公开构建里预置的同步服务器域名。自托管软件不该有「官方服务器」。

## v0.10.3 — 2026-09-02

- **6 位配对码**：手机上只输一串数字即可连上电脑，不用填地址和密码。
- 「开启同步」「添加设备」从命令面板移到设置里。
- 修复服务端两个会让配对码当场作废的缺陷（重试计数恒真、反向代理后限速桶只有一个）。

## v0.10.2 — 2026-09-02

- **修复正文不换行**（编辑器缺少软换行扩展，长段落只能横向滚动）。
- **修复侧栏拖不动文件**（Tauri 的系统级拖放会禁掉 WebView 内的 HTML5 拖放）。
- **修复点链接会把整个应用导航走**（白屏后只能重启）。
- 删掉移动端上下重复的按钮；修复移动端长按操作单从未渲染。
- 安卓改用固定签名密钥（此前每发一版换一把，用户每次更新都被迫卸载重装）。
- 未登录也能新建笔记库；设置里新增「存储位置」。

## v0.10.1 — 2026-08-30

- 更换全平台 logo。
- **内联标题**：文件名即标题，改标题就是改文件名。
- 修复打开笔记时标题行顶着一个 `#`；修复表格右对齐失效；文件树补上层级引导线。

## v0.10.0 — 2026-08-30

- 视觉语言与信息架构重做，桌面与移动端一起对标 Obsidian：侧栏收敛为「库名 + 一行图标 + 文件树」，同步状态下沉到状态栏，移动端改为 5 格常驻导航 + 可展开格式条，右栏改为「大纲 / 反向链接」标签页。

## v0.9.2 — 2026-08-29

- **同步状态面板**：说得出「现在还差什么」，而不是只有一个「同步」按钮。
- 修复「同步成功、上传 0、笔记永远上不去」（藏了七个版本的静默失败）。

## v0.9.1 — 2026-08-29

- 端到端补课：修复三个 P0，「登录 → 同步 → 另一端拉到」这条核心链路此前从未真正跑通。

## v0.9.0 — 2026-08-29

- 修复一个从 v0.2.0 潜伏至今的登录崩溃。

## v0.8.x — 2026-08-29

- **P2 高频体验**：侧栏与右栏可拖拽调宽、编辑区左右分栏、文内查找替换、最近打开与快速切换、侧栏搜索带上下文预览、设置页集中（外观/编辑器/同步/快捷键/关于）、移动可撤销。
- **实时预览扩展**：表格、callout、脚注、分隔线。
- **移动端补课**：新建文件夹入口、全库正文搜索、标签面板。
- **服务端 MCP endpoint** 与 `notes_write`：笔记库可直接作为 Agent 的读写终端。

## v0.7.x — 2026-08-26 ~ 08-29

- **检索引擎重写**：倒排索引 + BM25，中文二元组分词，索引快照持久化；此前是全库小写化后 `includes` 全扫。
- **修复全库正文索引形同虚设**：旧实现只在打开命令面板时建一次，导致反向链接在移动端从未显示过。
- **设计系统落地**：token / 排版 / 平面层次三层拆分，编辑态与阅读态共用同一套文字规则。
- **侧栏拖拽移动**文件与文件夹；**文件监听**，外部编辑器改同一目录能感知。
- 移动端交互对标 Obsidian：长按操作单、可折叠文件树、边缘手势、阅读模式活预览、图片 lightbox。
- 图谱视图、标签面板、`[[双链]]` 与补全、粘贴/拖入图片。

## v0.3.x — 2026-08-25 ~ 08-26

- **免登录本地模式**：不注册也能完整使用，登录改为按需。
- 修复 Windows 上「除文本输入外按钮全点不动」的五个根因。
- 编辑器统一升级为 CodeMirror 6（移动端此前是裸 textarea），阅读模式、排序、图片与 PDF 支持。
- 安卓 APK 由 CI 自动打包并签名。

## v0.1.0 ~ v0.2.0 — 2026-08-24

- 同步协议与一致性场景（C1~C8）、Go 服务端（auth / vault / sync / blob / WebSocket）、部署编排。
- 桌面端核心：CodeMirror 编辑器、笔记库管理、增量同步引擎（3-way diff3 合并、删改复活、冲突副本、墓碑去重）。
