<?xml version="1.0" encoding="utf-8"?>
<!-- 待办的一行：圈（点了勾掉）+ 事项（点了打开那篇）+ 所在笔记 -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="@dimen/ivw_row_h"
    android:orientation="horizontal"
    android:gravity="center_vertical">

    <ImageView
        android:id="@+id/ivw_row_check"
        android:layout_width="34dp"
        android:layout_height="@dimen/ivw_row_h"
        android:paddingStart="0dp"
        android:paddingEnd="12dp"
        android:paddingTop="6dp"
        android:paddingBottom="6dp"
        android:src="@drawable/ivw_ic_ring"
        android:contentDescription="@string/ivw_todo_done" />

    <LinearLayout
        android:id="@+id/ivw_row"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <TextView
            android:id="@+id/ivw_row_text"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/ivw_preview_todo"
            android:textColor="@color/ivw_text"
            android:textSize="14sp"
            android:maxLines="1"
            android:ellipsize="end" />

        <TextView
            android:id="@+id/ivw_row_note"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="10dp"
            android:maxWidth="96dp"
            android:text="@string/ivw_preview_title"
            android:textColor="@color/ivw_muted"
            android:textSize="11sp"
            android:maxLines="1"
            android:ellipsize="end" />
    </LinearLayout>
</LinearLayout>
