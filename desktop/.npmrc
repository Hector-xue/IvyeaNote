# 必须钉死公共 registry，不要改成任何镜像。
#
# 原因：本机 ~/.npmrc 指向内网镜像（mirrors.tencentyun.com），而 npm 会把解析到的
# 地址原样写进 package-lock.json 的 resolved 字段。一旦 lockfile 里存的是镜像地址，
# 别人 clone 下来 / CI 在 GitHub 跑，都会去访问一个他们根本到不了的主机，
# 表现为 npm 卡死或 "Exit handler never called" 这种看不出根因的假成功。
#
# 本地想用镜像加速，请只改 ~/.npmrc，不要改这个文件。
registry=https://registry.npmjs.org/
